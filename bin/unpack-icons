#!/bin/sh

#
# See setup for user tweakables.
#
. ./setup

# nothing to do if they are already unpacked
test -d $BUILDDIR/$OOO_ICONS_VER && exit 0;

echo "Unpacking icons..."

cd $BUILDDIR

tar ${OOO_ICONS_UNTAR_OPTIONS} $SRCDIR/$OOO_ICONS_TARBALL || exit 1;

echo "done"

exit 0;
