** Notes on back-port of VCL HEAD code: **

* Background:

    With the switch to HEAD in the next few months, we can afford to
make a huge / unmanageable patch - for the short term.

* TODO

Qt / KDE
    + split out the code into a plugin.

Win32
    + back-port the win32 theme stuff ...

Misc:
    ENABLE_GTK=yes - or whatever :-)
    gui-scale-ruler.diff
    gui-splash-redraw.diff
    gui-splash-takeover.diff
    system-freetype.diff
    system-xrender.diff
    Disable X fonts [ etc. ! ]

fpicker:
    + "can't get value on button / list"
    + test 'Save As' & Save
    + disable fpicker-gtk if not using gtk+ VCLPlug [ at run-time ]
    + huge filter names
    + huge image previews


* Changes to review / remove:

* Problem areas
	+ Printing changes can die ...
	+ source/gdi: dodgyness:
	    + impimage.cxx, impgraph.cxx
	    + image.cxx, graph.hxx
	    + gfxlink.cxx, bmpacc.cxx, bitmapex.cxx,
	    + bitmap3.cxx, bitmap2.cxx, bitmap.cxx
	+ inc/image.hxx, inc/image.h
	+ inc/graph.hxx, inc/bmpacc.hxx
    
-#ifndef REMOTE_APPSERVER
	 type removals - mae the patch bug / cumbersome (?)
	 source/gdi/opengl.cxx - particularly huge / uninteresting.

-#ifdef __BIGENDIAN
+#ifdef OSL_BIGENDIAN
	[ PORTABILITY ! ]

-#ifndef __SGI_STL_FUNCTIONAL
+#ifndef _STLP_FUNCTIONAL


* Merge features:

cvs diff -u -r OpenOffice_1_1_2 -r SRC680_m46 vcl

vcl/
    win/
    aqua/
    os2/
    mac/
    workben/
    util/defs/
    source/remote/
plain removals:
      source/app/oldsv.cxx
      inc/xevthdl.hxx
      inc/window.imp
      inc/vector2d.hxx
      inc/scrbar.imp
      inc/rvp.hxx
      inc/rversion.h
      inc/rmwindow.hxx
      inc/rmsound.hxx
      inc/rmprint.hxx
      inc/rmoutdev.hxx
      inc/rmevents.hxx
      inc/rmcache.hxx
      inc/resary.hxx
      inc/outdev.imp
      inc/ooffice.hxx
      inc/line.hxx
      inc/access.hxx
copied in:
      vcl/unx/gtk/
      vcl/unx/dummy/
      vcl/inv/plugins/

