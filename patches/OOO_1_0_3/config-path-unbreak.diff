Index: config_office/set_soenv.in
===================================================================
RCS file: /cvs/oo/tools/config_office/set_soenv.in,v
retrieving revision 1.3.2.2.2.8.2.2.2.3
diff -u -p -u -r1.3.2.2.2.8.2.2.2.3 set_soenv.in
--- config_office/set_soenv.in	3 Apr 2003 04:52:43 -0000	1.3.2.2.2.8.2.2.2.3
+++ config_office/set_soenv.in	28 Apr 2003 11:03:43 -0000
@@ -1034,7 +1034,7 @@ if ($platform eq "$Solaris") 
       $PATH .=          $ps.'$JAVA_HOME'.$BIN;
    }
 
-   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
+#   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
 
    $PATH             .= $ps.$oldPATH;
 }
@@ -1051,7 +1051,7 @@ elsif (($platform eq "$Linux") or ($plat
       $PATH .=          $ps.'$JAVA_HOME'.$BIN;
    }
 
-   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
+#   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
 
    $PATH             .= $ps.$oldPATH;
 }
