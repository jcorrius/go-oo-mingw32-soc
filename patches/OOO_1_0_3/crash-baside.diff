? basctl/unxlngi4.pro
Index: basctl/source/basicide/basides3.cxx
===================================================================
RCS file: /cvs/oo/script/basctl/source/basicide/basides3.cxx,v
retrieving revision 1.9
diff -u -p -u -r1.9 basides3.cxx
--- basctl/source/basicide/basides3.cxx	25 Sep 2001 09:10:51 -0000	1.9
+++ basctl/source/basicide/basides3.cxx	7 May 2003 15:52:45 -0000
@@ -170,11 +170,14 @@ DialogWindow* BasicIDEShell::CreateDlgWi
 		DBG_ASSERT( nKey, "CreateDlgWin: Kein Key - Fenster nicht gefunden!" );
 	}
 
-	pWin->GrabScrollBars( &aHScrollBar, &aVScrollBar );
-	pTabBar->InsertPage( (USHORT)nKey, aDlgName );
-	pTabBar->Sort();
-	if ( !pCurWin )
-		SetCurWindow( pWin, FALSE, FALSE );
+	if ( pWin )
+	{
+		pWin->GrabScrollBars( &aHScrollBar, &aVScrollBar );
+		pTabBar->InsertPage( (USHORT)nKey, aDlgName );
+		pTabBar->Sort();
+		if ( !pCurWin )
+			SetCurWindow( pWin, FALSE, FALSE );
+	}
 
 	bCreatingWindow = FALSE;
 	return pWin;
