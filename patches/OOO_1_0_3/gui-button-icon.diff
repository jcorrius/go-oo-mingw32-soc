Index: vcl/source/control/button.cxx
===================================================================
RCS file: /cvs/oo/gsl/vcl/source/control/button.cxx,v
retrieving revision 1.10
diff -u -p -u -r1.10 button.cxx
--- vcl/source/control/button.cxx	8 Nov 2001 16:05:45 -0000	1.10
+++ vcl/source/control/button.cxx	27 Mar 2003 17:32:00 -0000
@@ -619,6 +619,10 @@ void PushButton::ImplDrawPushButtonConte
             }
             else
             {
+		if ( mnButtonState & (BUTTON_DRAW_CHECKED | BUTTON_DRAW_DONTKNOW) )
+			pDev->SetFillColor( rStyleSettings.GetCheckedColor() );
+		else
+			pDev->SetFillColor( rStyleSettings.GetFaceColor() );
                 if ( IsZoom() )
                     pDev->DrawImage( aImagePos, aImageSize, maImage, nStyle );
                 else
