Index: vcl/source/control/ilstbox.cxx
===================================================================
RCS file: /cvs/oo/gsl/vcl/source/control/ilstbox.cxx,v
retrieving revision 1.25
diff -u -p -u -r1.25 ilstbox.cxx
--- vcl/source/control/ilstbox.cxx	8 Nov 2001 15:19:05 -0000	1.25
+++ vcl/source/control/ilstbox.cxx	27 May 2003 09:59:06 -0000
@@ -1519,6 +1519,7 @@ void ImplListBoxWindow::ImplPaint( USHOR
 	long nY = ( nPos - mnTop ) * mnMaxHeight;
 	Rectangle aRect( Point( 0, nY ), Size( nWidth, mnMaxHeight ) );
 
+	SetFillColor( GetBackground().GetColor() );
 	if( IsEnabled() )
 	{
 		if( mpEntryList->IsEntryPosSelected( nPos ) )
@@ -2311,6 +2312,7 @@ void ImplWin::Paint( const Rectangle& rR
 {
 	const StyleSettings& rStyleSettings = GetSettings().GetStyleSettings();
 
+	SetFillColor( GetBackground().GetColor() );
 	if( IsEnabled() )
 	{
 		if( HasFocus() )
