Index: sfx2/source/toolbox/tbxcust.cxx
===================================================================
RCS file: /cvs/oo/framework/sfx2/source/toolbox/tbxcust.cxx,v
retrieving revision 1.8.6.2
diff -u -p -u -r1.8.6.2 tbxcust.cxx
--- sfx2/source/toolbox/tbxcust.cxx	31 May 2002 11:08:56 -0000	1.8.6.2
+++ sfx2/source/toolbox/tbxcust.cxx	1 May 2003 13:02:17 -0000
@@ -575,9 +575,7 @@ SfxUserBitmapDialog_Impl::SfxUserBitmapD
 	aDescEdit.SetText( pToolBox->GetItemText( nFuncId ) );
     EnterWait();
 
-	VirtualDevice aDev;
     Size aSize = pImageMgr->GetImageSize();
-	aDev.SetOutputSizePixel(aSize);
 
 	// Das angegebene Verzeichnis nach Bitmaps durchsuchen
 	const String aBitmapExt( DEFINE_CONST_UNICODE( "bmp" ) );
@@ -604,8 +602,10 @@ SfxUserBitmapDialog_Impl::SfxUserBitmapD
 			}
 			else
 			{
-				aDev.DrawBitmap( Point(), aSize, *pBmp );
-				Image aImage( aDev.GetBitmap( Point(), aSize ), aColor );
+				Bitmap sBmp( *pBmp );
+				sBmp.Scale( aSize, BMP_SCALE_INTERPOLATE );
+				Image aImage( sBmp, aColor );
+
 				aSymbolTb.InsertItem( nItem, aBmpName );
 				aSymbolTb.SetItemImage( nItem, aImage );
 			}
