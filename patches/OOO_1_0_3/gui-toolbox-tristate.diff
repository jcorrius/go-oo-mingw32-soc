diff -u -p -u -r1.9.4.2 toolbox.cxx
--- vcl/source/window/toolbox.cxx
+++ vcl/source/window/toolbox.cxx
@@ -2784,9 +2786,6 @@ void ToolBox::ImplDrawItem( USHORT nPos,
         if ( !pItem->mbEnabled || !IsEnabled() )
             nImageStyle |= IMAGE_DRAW_DISABLE;
  
-        if ( pItem->meState == STATE_DONTKNOW )
-            nImageStyle |= IMAGE_DRAW_DISABLE;
-
         // Image ausgeben
         nTempOffX = nOffX;
         nTempOffY = nOffY;
