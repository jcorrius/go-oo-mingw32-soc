--- config_office/set_soenv.1	Wed Oct 30 07:39:09 2002
+++ config_office/set_soenv.1	Wed Jan 29 19:18:10 2003
@@ -948,11 +948,11 @@
 elsif (($platform eq "$Linux") || ($platform eq "$FreeBSD") || ($platform eq "$NetBSD") || ($platform eq "$Tru64"))
 {  $LD_LIBRARY_PATH   = $cur_dir.
                         $ps.'$SOLARENV'.$ds.'$INPATH'.$LIB.
-                        $ps.$USR_LOCAL.$LIB64.
                         $ps.$par_dir.$LIB.
                         $ps.$par_dir.$LIB64.
 			$ps_STLPORT_LIB.
-                        $ps.'$SOLARVER'.$ds.'$UPD'.$ds.'$INPATH'.$LIB;
+                        $ps.'$SOLARVER'.$ds.'$UPD'.$ds.'$INPATH'.$LIB.
+			$ps.$ENV{'LD_LIBRARY_PATH'};
 }
 elsif ($platform eq "$Irix" || $platform eq "$Irix64")
 {  $LD_LIBRARY_PATH   = $cur_dir.
@@ -1015,11 +1015,7 @@
                         $L.$USR_OPENWIN.$LIB.'"';
 }
 elsif (($platform eq "$Linux") || ($platform eq "$FreeBSD") || ($platform eq "$NetBSD") ||  ($platform eq "$Tru64") || ($platform eq "$Irix") || ($platform eq "$Irix64"))
-{  $SOLAREXTRALIB     = '"'.$L.$par_dir.$LIB.
-                        $L.$par_dir.$LIB64.
-                        $L.$LIB64.
-                        $L.$USR.$LIB64.
-                        $L.$USR.$LOCAL.$LIB64.'"';
+{  $SOLAREXTRALIB     = '"'.$L.$par_dir.$LIB.'"';
 }
 elsif ($platform eq "$Macosx") 
 {  $SOLAREXTRALIB     = '"'.$L.$LIB.
@@ -1101,19 +1097,7 @@
                         $ps.'$SOLARVER'.$ds.'$UPD'.$ds.'$INPATH'.$BIN.
                         $ps.'$SOLARENV'.$ds.'$OUTPATH'.$BIN.
                         $ps.'$SOLARENV'.$BIN;
-
-   if ( $JAVA_HOME ne "" )
-   {
-      $PATH .=          $ps.'$JAVA_HOME'.$BIN;
-   }
-
-   $PATH             .= GetCorrectPath($COMPATH, $PERL_PATH, $TCSH_PATH, $compiler);
-
-   $PATH             .= $ps.$USR.$ds."sbin".
-                        $ps.$ETC.
-                        $ps.$USR.$BIN.$ds."X11".
-			$ps.$USR.$ds."X11".$BIN.
-                        $ps.$USR.$LIB64;
+   $PATH             .= $ps.$oldPATH;
 }
 elsif ($platform eq "$Winnt")
 { 
@@ -1222,9 +1206,6 @@
 			$L.'$SOLARENV'.$ds.'$OUTPATH'.$LIB.
                         $L.'$SOLARVER'.$ds.'$UPD'.$ds.'$INPATH'.$LIB.
                         $L.'$SOLARENV'.$ds.'$OUTPATH'.$LIB.
-                        $L.$LIB64.
-                        $L.$USR_LIB.
-                        $L.$USR_LOCAL.$LIB64.
                         $L.'$JAVA_HOME'.$LIB.
                         $L.$JRELIBDIR.
                         $L.$JRETOOLKITDIR.
@@ -1321,9 +1302,7 @@
 
 #The tail, if needed
 if (($platform eq "$Linux") or  ($platform eq "$FreeBSD") or ($platform eq "$NetBSD"))
-{  $SOLARINC         .= $I.$XINC.
-                        $I.$GCC_INCLUDE.
-                        $I.$USR.$INCLUDE;
+{  $SOLARINC         .= $I.$XINC;
 }
 elsif ($platform eq "$Tru64") 
 {  $SOLARINC         .= $I.$XINC.
