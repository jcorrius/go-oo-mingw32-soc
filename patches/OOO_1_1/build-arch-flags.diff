--- python/Python-2.2.2.patch
+++ python/Python-2.2.2.patch
@@ -1748,13 +1748,13 @@
 !     Linux*)
 !         case "`uname -m`" in
 ! 	    ppc*)
-!     	       OPT="$OPT -fPIC";;
+!     	       OPT="$OPT -fPIC $ARCH_FLAGS";;
 ! 	    sparc64*)
-!     	       OPT="$OPT -fPIC";;
+!     	       OPT="$OPT -fPIC $ARCH_FLAGS";;
 !           x86_64*)
-!              OPT="$OPT -fPIC";;
+!              OPT="$OPT -fPIC $ARCH_FLAGS";;
 !             *)
-!     	       OPT="$OPT";;
+!     	       OPT="$OPT -fPIC $ARCH_FLAGS";;
 ! 	esac
 !         ;;
       esac
--- solenv/inc/unxlngi4.mk
+++ solenv/inc/unxlngi4.mk
@@ -98,15 +98,20 @@
 CFLAGSENABLESYMBOLS=-g
 .ENDIF
 
+# define default arch flags when needed
+.IF "$(ARCH_FLAGS)"==""
+ARCH_FLAGS=-mcpu=pentiumpro
+.ENDIF
+
 # flags for the C++ Compiler
-CFLAGSCC= -pipe -mcpu=pentiumpro
+CFLAGSCC= -pipe $(ARCH_FLAGS)
 # Flags for enabling exception handling
 CFLAGSEXCEPTIONS=-fexceptions -fno-enforce-eh-specs
 # Flags for disabling exception handling
 CFLAGS_NO_EXCEPTIONS=-fno-exceptions
 
 # -fpermissive should be removed as soon as possible
-CFLAGSCXX= -pipe -mcpu=pentiumpro -fno-for-scope -fpermissive -fno-rtti
+CFLAGSCXX= -pipe $(ARCH_FLAGS) -fno-for-scope -fpermissive -fno-rtti
 
 # HACK: enable Hamburg developers to build on glibc-2.2 machines but compile vs. glibc-2.1 headers
 .IF "$(BUILD_SPECIAL)"==""
--- stlport/STLport-4.5.patch
+++ stlport/STLport-4.5.patch
@@ -186,7 +186,7 @@
 ! 
 ! WARNING_FLAGS= -Wall -W -Wno-sign-compare -Wno-unused -Wno-uninitialized -ftemplate-depth-32
 ! 
-! CXXFLAGS_COMMON =  -D_REENTRANT -DGXX_INCLUDE_PATH=${GXX_INCLUDE_PATH} -fexceptions -I${STLPORT_DIR} ${WARNING_FLAGS}
+! CXXFLAGS_COMMON =  -D_REENTRANT -DGXX_INCLUDE_PATH=${GXX_INCLUDE_PATH} -fexceptions -I${STLPORT_DIR} ${WARNING_FLAGS} ${ARCH_FLAGS}
 ! 
 ! CXXFLAGS_RELEASE_static = $(CXXFLAGS_COMMON) -O2 -fPIC
 ! CXXFLAGS_RELEASE_dynamic = $(CXXFLAGS_COMMON) -O2 -fPIC
