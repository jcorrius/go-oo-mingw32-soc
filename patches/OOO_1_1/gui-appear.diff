Index: desktop/source/app/app.cxx
===================================================================
RCS file: /cvs/framework/desktop/source/app/app.cxx,v
retrieving revision 1.121.4.1
diff -u -p -u -r1.121.4.1 app.cxx
--- desktop/source/app/app.cxx	5 Aug 2003 11:27:25 -0000	1.121.4.1
+++ desktop/source/app/app.cxx	29 Aug 2003 13:37:27 -0000
@@ -2245,6 +2245,7 @@ void Desktop::HandleAppEvent( const Appl
     }
     else if ( rAppEvent.GetEvent() == "APPEAR" && !GetCommandLineArgs()->IsInvisible() )
     {
+#if 0
         // find active task - the active task is always a visible task
         ::com::sun::star::uno::Reference< ::com::sun::star::frame::XFramesSupplier >
                 xDesktop( ::comphelper::getProcessServiceFactory()->createInstance( OUSTRING(RTL_CONSTASCII_USTRINGPARAM("com.sun.star.frame.Desktop")) ),
@@ -2264,6 +2265,7 @@ void Desktop::HandleAppEvent( const Appl
             xTop->toFront();
         }
         else
+#endif
             // no visible task that could be activated found
             OpenDefault();
     }
Index: desktop/source/app/officeipcthread.cxx
===================================================================
RCS file: /cvs/framework/desktop/source/app/officeipcthread.cxx,v
retrieving revision 1.36
diff -u -p -u -r1.36 officeipcthread.cxx
--- desktop/source/app/officeipcthread.cxx	10 Jul 2003 14:41:29 -0000	1.36
+++ desktop/source/app/officeipcthread.cxx	17 Sep 2003 16:32:01 -0000
@@ -613,7 +613,7 @@ void SAL_CALL OfficeIPCThread::run()
 				bDocRequestSent |= aCmdLineArgs.GetForceNewList( pRequest->aForceNewList );
 
 				// Special command line args to create an empty document for a given module
-                if ( aCmdLineArgs.HasModuleParam() && Desktop::CheckOEM())
+                if ( !bDocRequestSent && aCmdLineArgs.HasModuleParam() && Desktop::CheckOEM())
 				{
 					SvtModuleOptions aOpt;
 					SvtModuleOptions::EFactory eFactory = SvtModuleOptions::E_WRITER;
Index: framework/source/services/frame.cxx
===================================================================
RCS file: /cvs/framework/framework/source/services/frame.cxx,v
retrieving revision 1.69
diff -u -p -u -r1.69 frame.cxx
--- framework/source/services/frame.cxx	27 Jun 2003 09:09:04 -0000	1.69
+++ framework/source/services/frame.cxx	24 Sep 2003 08:07:36 -0000
@@ -1563,8 +1563,10 @@ sal_Bool SAL_CALL Frame::setComponent(  
     // Set this information on it!
     if ( xComponentWindow.is() && m_eActiveState == E_FOCUS )
     {
+#if 0
         if ( bHadFocus )
             xComponentWindow->setFocus();
+#endif
 
 #ifdef ENABLE_DEFMODALDIALOGPARENT
         /* SOLAR SAFE { */
Index: framework/source/dispatch/basedispatcher.cxx
===================================================================
RCS file: /cvs/framework/framework/source/dispatch/basedispatcher.cxx,v
retrieving revision 1.39
diff -u -p -u -r1.39 basedispatcher.cxx
--- framework/source/dispatch/basedispatcher.cxx	22 Jul 2003 11:05:57 -0000	1.39
+++ framework/source/dispatch/basedispatcher.cxx	24 Sep 2003 08:31:26 -0000
@@ -930,12 +930,14 @@ void BaseDispatcher::implts_enableFrame(
             sal_Bool bHidden = sal_False;
             aAnalyzer.getArgument( E_HIDDEN, bHidden );
             xFrameWindow->setVisible( !bHidden );
+#if 0
             if (!bHidden)
             {
                 css::uno::Reference< css::awt::XTopWindow > xTopWindow ( xFrameWindow, css::uno::UNO_QUERY );
                 if(xTopWindow.is())
                     xTopWindow->toFront();
             }
+#endif
         }
 
         // Attention: Two cases could occure.
