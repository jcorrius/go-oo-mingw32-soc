Index: sw/source/ui/app/appopt.cxx
===================================================================
RCS file: /cvs/sw/sw/source/ui/app/appopt.cxx,v
retrieving revision 1.21
diff -u -p -u -r1.21 appopt.cxx
--- sw/source/ui/app/appopt.cxx	17 Apr 2003 15:10:58 -0000	1.21
+++ sw/source/ui/app/appopt.cxx	12 Aug 2003 11:40:37 -0000
@@ -174,6 +174,9 @@
 #include <svx/unolingu.hxx>
 #endif
 
+#include <osl/process.h>
+#include <rtl/locale.hxx>
+
 #ifndef _GLOBALS_HRC
 #include <globals.hrc>
 #endif
@@ -284,6 +287,21 @@ SfxItemSet*	 SwModule::CreateItemSet( US
         SwLinguConfig aLinguCfg;
 
         Any aLang = aLinguCfg.GetProperty(C2U("DefaultLocale"));
+
+#ifdef UNX
+	rtl::OUString aStrLocale;
+	aLang >>= aStrLocale;
+	if (!aStrLocale.getLength()) {
+		rtl_Locale *pRtlLocale = NULL;
+		osl_getProcessLocale( &pRtlLocale );
+		rtl::OLocale aOLocale( pRtlLocale );
+		rtl::OUString aSysLang = ( aOLocale.getLanguage() + 
+					   rtl::OUString::createFromAscii( "-" ) +
+					   aOLocale.getCountry() );
+		aLang <<= aSysLang;
+	}
+#endif
+
         Locale aLocale;
         aLang >>= aLocale;
         pRet->Put(SvxLanguageItem(SvxLocaleToLanguage( aLocale ), SID_ATTR_LANGUAGE));
Index: svtools/source/misc/lingucfg.cxx
===================================================================
RCS file: /cvs/util/svtools/source/misc/lingucfg.cxx,v
retrieving revision 1.12
diff -u -p -u -r1.12 lingucfg.cxx
--- svtools/source/misc/lingucfg.cxx	27 Mar 2003 14:39:15 -0000	1.12
+++ svtools/source/misc/lingucfg.cxx	12 Aug 2003 11:52:35 -0000
@@ -520,7 +520,13 @@ BOOL SvtLinguConfig::LoadConfig()
 			{
 				switch ( i )
 				{
-                    case  0: { aOpt.bRODefaultLanguage = pROStates[i]; aOpt.nDefaultLanguage = lcl_CfgAnyToLanguage( rVal ); } break;
+                    case  0: { aOpt.bRODefaultLanguage = pROStates[i];
+#ifdef UNX
+			       aOpt.nDefaultLanguage = GetSystemUILanguage();
+#else
+			       aOpt.nDefaultLanguage = lcl_CfgAnyToLanguage( rVal );
+#endif
+		    } break;
                     case  1: { aOpt.bROActiveDics = pROStates[i]; rVal >>= aOpt.aActiveDics;   } break;
                     case  2: { aOpt.bROIsUseDictionaryList = pROStates[i]; rVal >>= aOpt.bIsUseDictionaryList;  } break;
                     case  3: { aOpt.bROIsIgnoreControlCharacters = pROStates[i]; rVal >>= aOpt.bIsIgnoreControlCharacters;    } break;
