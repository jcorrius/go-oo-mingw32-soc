Index: sfx2/prj/build.lst
===================================================================
RCS file: /cvs/framework/sfx2/prj/build.lst,v
retrieving revision 1.5
diff -u -p -u -r1.5 build.lst
--- sfx2/prj/build.lst	27 May 2003 12:00:03 -0000	1.5
+++ sfx2/prj/build.lst	7 Apr 2004 16:17:11 -0000
@@ -1,4 +1,4 @@
-sf	sfx2	:	idl setup2 xmloff xmlscript framework shell NULL
+sf	sfx2	:	idl setup2 xmloff xmlscript framework shell sj2 NULL
 sf	sfx2									usr1	-	all	sf_mkout NULL
 sf	sfx2\inc								get		-	all	sf_inc NULL
 sf	sfx2\prj								get		-	all	sf_prj NULL
Index: sfx2/util/makefile.mk
===================================================================
RCS file: /cvs/framework/sfx2/util/makefile.mk,v
retrieving revision 1.31
diff -u -p -u -r1.31 makefile.mk
--- sfx2/util/makefile.mk	22 Apr 2003 16:34:07 -0000	1.31
+++ sfx2/util/makefile.mk	7 Apr 2004 16:17:11 -0000
@@ -125,10 +125,7 @@ SHL1STDLIBS+=\
 		$(VOSLIB) \
 		$(SALLIB)
 
-.IF "$(SOLAR_JAVA)" != ""
-SHL1STDLIBS+=\
-		$(SJLIB)
-.ENDIF # SOLAR_JAVA
+SHL1STDLIBS+=$(SJLIB)
 
 .IF "$(GUI)"=="WNT"
 
Index: scp/source/global/setupzip.scp
===================================================================
RCS file: /cvs/installation/scp/source/global/Attic/setupzip.scp,v
retrieving revision 1.114.10.5
diff -u -p -u -r1.114.10.5 setupzip.scp
--- scp/source/global/setupzip.scp	28 Jan 2004 17:18:13 -0000	1.114.10.5
+++ scp/source/global/setupzip.scp	14 Apr 2004 09:46:53 -0000
@@ -390,8 +390,6 @@ STD_SETUPZIP_RES_FILE( GID_FILE_RES_TK, 
 STD_SETUPZIP_RES_FILE( GID_FILE_RES_UUI, uui )
 STD_SETUPZIP_RES_FILE( GID_FILE_RES_VCL, vcl )
 
-#if !defined(IPL_PRODUCT) && !defined(OPL_PRODUCT) || defined(UNX)
-
 File GID_FILE_LIB_J
 	TXT_FILE_BODY;
 	#ifdef UNX
@@ -403,8 +401,6 @@ File GID_FILE_LIB_J
 	Styles			= (PACKED, SETUPZIP);
 End
 
-#endif
-
 File GID_FILE_LIB_COMPHELPER2
 	TXT_FILE_BODY;
 	#ifdef UNX
