Index: config_office/set_soenv.in
===================================================================
RCS file: /cvs/tools/config_office/set_soenv.in,v
retrieving revision 1.6
diff -u -p -u -r1.6 set_soenv.in
--- config_office/set_soenv.in	28 Apr 2003 17:02:16 -0000	1.6
+++ config_office/set_soenv.in	13 May 2003 09:58:42 -0000
@@ -973,7 +973,7 @@ if ($platform eq "$Solaris") 
       $PATH .=          $ps.'$JAVA_HOME'.$BIN;
    }
 
-   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
+#   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
 
    $PATH             .= $ps.$oldPATH;
 }
@@ -996,7 +996,7 @@ elsif (($platform eq "$Linux") or ($plat
       $PATH .=          $ps.'$JAVA_HOME'.$BIN;
    }
 
-   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
+#   $PATH             .= GetCorrectPath($COMPATH.$BIN, $PERL_PATH, $TCSH_PATH, $compiler);
 
 }
 
