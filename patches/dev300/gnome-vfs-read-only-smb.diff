--- ucb/source/ucp/gvfs/content.cxx	2007-10-26 16:20:12.000000000 +0200
+++ ucb/source/ucp/gvfs/content.cxx	2007-10-26 16:20:01.000000000 +0200
@@ -1618,8 +1618,12 @@ Content::createStream( const uno::Refere
 	}
 
 	if (result == GNOME_VFS_ERROR_INVALID_OPEN_MODE ||
-	    result == GNOME_VFS_ERROR_NOT_SUPPORTED)
+	    result == GNOME_VFS_ERROR_NOT_SUPPORTED ||
+	    result == GNOME_VFS_ERROR_ACCESS_DENIED)
+	{
+		m_bForceReadOnly = sal_True;
 		return xStream;
+	}
 	else if (result == GNOME_VFS_ERROR_LOCKED )
 		throwLocked( xEnv );
 	else if (result != GNOME_VFS_OK)
