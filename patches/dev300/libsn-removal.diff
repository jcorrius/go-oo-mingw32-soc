diff --git a/config_office/configure.in b/config_office/configure.in
index 8f92cbc..18018b9 100644
--- config_office/configure.in
+++ config_office/configure.in
@@ -63,9 +63,6 @@ AC_ARG_WITH(vba-package-format,
 AC_ARG_ENABLE(pch,
 [  --enable-pch            EXPERIMENTAL: Enables precompiled header support for C++.
 ],,)
-AC_ARG_ENABLE(libsn,
-[  --enable-libsn          Enables the use of libstartup-notification
-],,)
 AC_ARG_ENABLE(mozilla,
 [  --disable-mozilla       OO.o usually includes a strangely hacked up mozilla
                           binary for your platform, to build without this
@@ -5304,22 +5301,6 @@ fi
 AC_SUBST(ENABLE_LOCKDOWN)
 
 dnl ===================================================================
-dnl Test for the presence of libstartup-notification
-dnl ===================================================================
-AC_MSG_CHECKING([whether to use libstartup-notification])
-WITH_LIBSN=NO
-if test -n "$enable_libsn" && test "$enable_libsn" != "no"; then
-  PKG_CHECK_MODULES( LIBSN, libstartup-notification-1.0 >= 0.5 )
-  WITH_LIBSN=YES
-  AC_MSG_RESULT([yes])
-else
-  AC_MSG_RESULT([no])
-fi
-AC_SUBST(WITH_LIBSN)
-AC_SUBST(LIBSN_CFLAGS)
-AC_SUBST(LIBSN_LIBS)
-
-dnl ===================================================================
 dnl Test whether to include Evolution 2 support
 dnl ===================================================================
 AC_MSG_CHECKING([whether to enable evolution 2 support])
diff --git a/config_office/set_soenv.in b/config_office/set_soenv.in
index 92d4ccb..9b08621 100644
--- config_office/set_soenv.in
+++ config_office/set_soenv.in
@@ -1871,9 +1871,6 @@ ToFile( "VC_STANDARD",       "@VC_STANDARD@",      "e" );
 ToFile( "WITH_GPC",          "@WITH_GPC@",         "e" );
 ToFile( "WITH_MYSPELL_DICTS","@WITH_MYSPELL_DICTS@","e");
 ToFile( "ENABLE_LOCKDOWN",   "@ENABLE_LOCKDOWN@",  "e" );
-ToFile( "WITH_LIBSN",        "@WITH_LIBSN@",       "e" );
-ToFile( "LIBSN_CFLAGS",      "@LIBSN_CFLAGS@",     "e" );
-ToFile( "LIBSN_LIBS",        "@LIBSN_LIBS@",       "e" );
 ToFile( "WITH_LDAP",         "@WITH_LDAP@",        "e" );
 ToFile( "WITH_OPENLDAP",     "@WITH_OPENLDAP@",    "e" );
 ToFile( "WITH_MOZILLA",      "@WITH_MOZILLA@",     "e" );
diff --git a/vcl/unx/source/app/makefile.mk b/vcl/unx/source/app/makefile.mk
index 94ee296..32c9e46 100644
--- vcl/unx/source/app/makefile.mk
+++ vcl/unx/source/app/makefile.mk
@@ -81,11 +81,6 @@ CDEFS+=-DSYSTEM_PORTAUDIO
             $(SLO)$/salsys.obj
 
 
-.IF "$(WITH_LIBSN)"=="YES"
-CDEFS+=-DHAVE_LIBSN
-CFLAGS+=$(LIBSN_CFLAGS)
-.ENDIF
-
 .IF "$(USE_XINERAMA)" != "NO"
 CDEFS+=-DUSE_XINERAMA
 .IF "$(USE_XINERAMA_VERSION)" == "Xorg"
diff --git a/vcl/unx/source/app/saldisp.cxx b/vcl/unx/source/app/saldisp.cxx
index b9efe40..0c9652c 100644
--- vcl/unx/source/app/saldisp.cxx
+++ vcl/unx/source/app/saldisp.cxx
@@ -86,11 +86,6 @@ Status XineramaGetInfo(Display*, int, XRectangle*, unsigned char*, int*);
 #endif
 #endif
 
-#ifdef HAVE_LIBSN
-#  define SN_API_NOT_YET_FROZEN
-#  include <libsn/sn.h>
-#endif
-
 #include <postx.h>
 
 #include <salunx.h>
@@ -560,30 +555,6 @@ BOOL SalDisplay::BestVisual( Display     *pDisplay,
 
 // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
 
-#ifdef HAVE_LIBSN
-extern "C" {
-    static void
-    SnErrorTrapPush( SnDisplay *display,
-		     Display   *xdisplay )
-    {
-        SalXLib *pXLib = GetX11SalData()->GetLib();
-        if (pXLib)
-            pXLib->PushXErrorLevel( true );
-    }
-
-    static void
-    SnErrorTrapPop( SnDisplay *display,
-		    Display   *xdisplay )
-    {
-        SalXLib *pXLib = GetX11SalData()->GetLib();
-        XSync( xdisplay, False ); // flush error queue
-        
-        if (pXLib)
-            pXLib->PopXErrorLevel();
-    }
-}
-#endif /* HAVE_LIBSN */
-
 SalDisplay::SalDisplay( Display *display ) :
         mpInputMethod( NULL ),
 		mpFallbackFactory ( NULL ),
@@ -634,16 +605,6 @@ void SalDisplay::doDestruct()
     X11SalGraphics::releaseGlyphPeer();
     DestroyFontCache();
 
-#ifdef HAVE_LIBSN
-    if( m_pSnLauncheeContext )
-    {
-        sn_launchee_context_complete( m_pSnLauncheeContext );
-        sn_launchee_context_unref( m_pSnLauncheeContext );
-	}
-    if ( m_pSnDisplay )
-        sn_display_unref( m_pSnDisplay );
-#endif /* HAVE_LIBSN */
-
     if( IsDisplay() )
     {
 		delete mpInputMethod, mpInputMethod = (SalI18N_InputMethod*)ILLEGAL_POINTER;
@@ -888,11 +849,7 @@ void SalDisplay::initScreen( int nScreen ) const
 }
 
 // -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
-void SalDisplay::Init( bool
-#ifdef HAVE_LIBSN
-bHandleStartupNotification
-#endif
-)
+void SalDisplay::Init( bool )
 {
     for( size_t i = 0; i < POINTER_COUNT; i++ )
         aPointerCache_[i] = None;
@@ -1105,19 +1062,6 @@ bHandleStartupNotification
     // initialize system settings update
     m_pDtIntegrator = DtIntegrator::CreateDtIntegrator();
 
-#ifdef HAVE_LIBSN
-    if ( bHandleStartupNotification )
-    {
-        m_pSnDisplay = sn_display_new( pDisp_, SnErrorTrapPush, SnErrorTrapPop );
-        m_pSnLauncheeContext = sn_launchee_context_new_from_environment( m_pSnDisplay, m_nDefaultScreen );
-#  ifdef DBG_UTIL
-        if( !m_pSnLauncheeContext )
-            fprintf( stderr, "Failed to get launch feedback info from "
-                    "DESKTOP_LAUNCH_ID/DESKTOP_LAUNCH_WINDOW\n" );
-#  endif /* DBG_UTIL */
-    }
-#endif /* HAVE_LIBSN */
-
 #ifdef DBG_UTIL
     PrintInfo();
 #endif
@@ -2365,18 +2309,6 @@ void SalX11Display::Yield()
 
     XNextEvent( pDisp_, &aEvent );
 
-#ifdef HAVE_LIBSN
-    if( m_pSnLauncheeContext )
-    {
-        sn_launchee_context_complete( m_pSnLauncheeContext );
-        sn_launchee_context_unref( m_pSnLauncheeContext );
-        m_pSnLauncheeContext = NULL;
-    }
-
-    if( m_pSnDisplay && sn_display_process_event( m_pSnDisplay, &aEvent ) )
-        return;
-#endif /* HAVE_LIBSN */
-
     Dispatch( &aEvent );
 
 #ifdef DBG_UTIL
diff --git a/vcl/util/makefile.mk b/vcl/util/makefile.mk
index b9d3e93..794dbc5 100644
--- vcl/util/makefile.mk
+++ vcl/util/makefile.mk
@@ -321,10 +321,6 @@ SHL2STDLIBS += -ldl -lnsl -lsocket
 
 .IF "$(GUIBASE)"=="unx"
 
-.IF "$(WITH_LIBSN)"=="YES"
-SHL2STDLIBS+=$(LIBSN_LIBS)
-.ENDIF
-
 SHL2STDLIBS += -lXext -lSM -lICE -lX11
 .IF "$(OS)"!="MACOSX" && "$(OS)"!="FREEBSD"
 # needed by salprnpsp.cxx
