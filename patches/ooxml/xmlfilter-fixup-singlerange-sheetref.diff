--- sc/source/core/tool/rangelst.cxx   	2007-10-23 11:09:55.000000000 +0100
+++ sc/source/core/tool/rangelst.cxx	2007-10-23 11:11:58.000000000 +0100
@@ -109,15 +109,23 @@ USHORT ScRangeList::Parse( const String&
 		for ( USHORT i=0; i<nTCount; i++ )
 		{
 			aOne = rStr.GetToken( i, cDelimiter );
+			aRange.aStart.SetTab( nTab );	// Default Tab wenn nicht angegeben
+			USHORT nRes = 0;
 			// FIXME : broken for Lotus
-			if ( aOne.Search( ':' ) == STRING_NOTFOUND )
-			{	// Range muss es sein
-				String aStrTmp( aOne );
-				aOne += ':';
-				aOne += aStrTmp;
+			if ( eConv == ScAddress::CONV_OOO )
+			{
+		
+				if ( aOne.Search( ':' ) == STRING_NOTFOUND )
+				{	// Range muss es sein
+					String aStrTmp( aOne );
+					aOne += ':';
+					aOne += aStrTmp;
+				}
+				nRes = aRange.Parse( aOne, pDoc, eConv );
 			}
-			aRange.aStart.SetTab( nTab );	// Default Tab wenn nicht angegeben
-			USHORT nRes = aRange.Parse( aOne, pDoc, eConv );
+			else
+				nRes = aRange.ParseAny( aOne, pDoc, eConv );
+	
 			if ( (nRes & nMask) == nMask )
 				Append( aRange );
 			nResult &= nRes;		// alle gemeinsamen Bits bleiben erhalten
