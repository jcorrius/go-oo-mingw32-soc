Index: rsc/source/parser/rscdb.cxx
===================================================================
RCS file: /cvs/gsl/rsc/source/parser/rscdb.cxx,v
retrieving revision 1.9
retrieving revision 1.8.12.2
diff -u -p -r1.9 -r1.8.12.2
--- rsc/source/parser/rscdb.cxx	26 Jun 2004 20:25:04 -0000	1.9
+++ rsc/source/parser/rscdb.cxx	14 Oct 2004 00:17:33 -0000	1.8.12.2
@@ -59,7 +59,6 @@
  *
  ************************************************************************/
 /****************** I N C L U D E S **************************************/
-
 #pragma hdrstop
 
 // C and C++ Includes.
@@ -659,16 +658,16 @@ void RscEnumerateObj :: WriteRcFile( Rsc
 		UINT32 nSize = (nCount * 2 + 1) * sizeof( INT32 );
 
 		rMem.Put( nCount ); //Anzahl speichern
-		INT32	l = (INT32)pTypCont->aIdTranslator.First();
+		INT32	l = (INT32)(sal_IntPtr)pTypCont->aIdTranslator.First();
 		while( nCount )
 		{
 			// Schluessel schreiben
 			l = pTypCont->aIdTranslator.GetCurKey();
 			rMem.Put( l );
 			// Objekt Id oder Position schreiben
-			l = (INT32)pTypCont->aIdTranslator.GetCurObject();
+			l = (INT32)(sal_IntPtr)pTypCont->aIdTranslator.GetCurObject();
 			rMem.Put( l );
-			l = (INT32)pTypCont->aIdTranslator.Next();
+			l = (INT32)(sal_IntPtr)pTypCont->aIdTranslator.Next();
 			nCount--;
 		}
 		rMem.Put( nSize ); // Groesse hinten Speichern
