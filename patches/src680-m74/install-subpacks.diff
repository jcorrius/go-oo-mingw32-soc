--- solenv/bin/make_installer.pl.old	2005-01-17 17:31:06.000000000 +0100
+++ solenv/bin/make_installer.pl	2005-01-20 17:42:23.000000000 +0100
@@ -1027,7 +1027,7 @@
 			# Simple installation mechanism
 			###########################################
 
-			if ( $installer::globals::simple ) { installer::worker::install_simple($onepackagename, $dirsinpackage, $filesinpackage, $linksinpackage); }
+			if ( $installer::globals::simple ) { installer::worker::install_simple($onepackagename, $$languagestringref, $dirsinpackage, $filesinpackage, $linksinpackage); }
 
 			###########################################
 			# Creating epm list file
--- solenv/bin/modules/installer/worker.pm.old	2004-12-16 11:44:50.000000000 +0100
+++ solenv/bin/modules/installer/worker.pm	2005-01-20 17:38:07.000000000 +0100
@@ -505,9 +505,9 @@
 # Mechanism for simple installation without packing
 ###########################################################
 
-sub install_simple ($$$$)
+sub install_simple ($$$$$)
 {
-	my ($packagename, $directoriesarray, $filesarray, $linksarray) = @_;
+	my ($packagename, $languagestring, $directoriesarray, $filesarray, $linksarray) = @_;
 
 	print "... installing module $packagename ...\n";
 
@@ -564,6 +564,7 @@
 	{
 		my $filelist;
 		my $fname = $installer::globals::destdir . "/$packagename";
+		if ($installer::globals::languagepack) { $fname .= ".$languagestring"; }
 		open ($filelist, ">$fname") || die "Can't open $fname: $!";
 		print $filelist @lines;
 		close ($filelist);
