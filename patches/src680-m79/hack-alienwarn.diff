Index: sfx2/source/dialog/alienwarn.cxx
===================================================================
RCS file: /cvs/framework/sfx2/source/dialog/alienwarn.cxx,v
retrieving revision 1.2
diff -u -p -u -r1.2 alienwarn.cxx
--- sfx2/source/dialog/alienwarn.cxx	2 Feb 2005 16:47:06 -0000	1.2
+++ sfx2/source/dialog/alienwarn.cxx	9 Mar 2005 12:30:57 -0000
@@ -101,6 +101,8 @@ SfxAlienWarningDialog::SfxAlienWarningDi
     // if the button text is too wide, then broaden the button
     long nTxtW = m_aMoreInfoBtn.GetTextWidth( m_aMoreInfoBtn.GetText() );
     long nCtrlW = m_aMoreInfoBtn.GetSizePixel().Width();
+	m_aMoreInfoBtn.Hide();
+#if 0
     if ( nTxtW >= nCtrlW )
     {
         long nDelta = nTxtW - nCtrlW;
@@ -120,6 +122,7 @@ SfxAlienWarningDialog::SfxAlienWarningDi
         aNewSize.Width() += nDelta;
         m_aMoreInfoBtn.SetPosSizePixel( aNewPoint, aNewSize );
     }
+#endif
 
     // text of checkbox to wide -> add new line
     nTxtW = m_aWarningOffBox.GetTextWidth( m_aWarningOffBox.GetText() );
