--- berkeleydb/makefile.mk.old	2005-03-30 10:33:28.000000000 +0200
+++ berkeleydb/makefile.mk	2005-04-27 16:12:27.000000000 +0200
@@ -95,7 +95,10 @@
 .IF "$(GUI)"=="UNX"
 CONFIGURE_DIR=out
 #relative to CONFIGURE_DIR
-CONFIGURE_ACTION=..$/dist$/configure
+CONFIGURE_ACTION= \
+	setenv CFLAGS "$(ARCH_FLAGS)" && \
+	setenv CXXFLAGS "$(ARCH_FLAGS)" && \
+	..$/dist$/configure
 CONFIGURE_FLAGS=--enable-cxx --enable-dynamic --enable-shared
 .IF "$(SOLAR_JAVA)"!=""
 CONFIGURE_FLAGS+=--enable-java
