Index: sysui/desktop/util/makefile.mk
--- sysui/desktop/util/makefile.mk  2005-02-11 16:44:16.000000000 +0100 
+++ sysui/desktop/util/makefile.mk  2005-02-11 16:44:16.000000000 +0100
@@ -81,7 +81,7 @@ LINKFLAGS+=-z nodefs
 
 # Disable lazy binding of symbols
 .IF "$(OS)" == "LINUX" || "$(OS)" == "SOLARIS"
-LINKFLAGS+=-z now
+LINKFLAGS+=-Wl,-z,now
 .ENDIF  
 
 .IF "$(OS)" == "LINUX"
