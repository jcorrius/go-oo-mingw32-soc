diff -urN config_office/configure.in config_office.new/configure.in
--- config_office/configure.in	2006-02-17 09:52:04.000000000 +0100
+++ config_office.new/configure.in	2006-02-27 14:58:05.000000000 +0100
@@ -4172,22 +4172,22 @@
 
 AC_MSG_CHECKING([whether to include PPDs])
 if test "$without_ppds" = "yes"; then
-  AC_MSG_RESULT([yes])
-else
   AC_MSG_RESULT([no])
   WITHOUT_PPDS=YES
   SCPDEFS="$SCPDEFS -DWITHOUT_PPDS"
+else
+  AC_MSG_RESULT([yes])
 fi
 AC_SUBST(WITHOUT_PPDS)
 AC_SUBST(SCPDEFS)
 
 AC_MSG_CHECKING([whether to include AFMs])
 if test "$without_afms" = "yes"; then
-  AC_MSG_RESULT([yes])
-else
   AC_MSG_RESULT([no])
   WITHOUT_AFMS=YES
   SCPDEFS="$SCPDEFS -DWITHOUT_AFMS"
+else
+  AC_MSG_RESULT([yes])
 fi
 AC_SUBST(WITHOUT_AFMS)
 AC_SUBST(SCPDEFS)
