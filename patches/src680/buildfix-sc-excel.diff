Index: sc/source/filter/excel/xeview.cxx
===================================================================
RCS file: /cvs/sc/sc/source/filter/excel/xeview.cxx,v
retrieving revision 1.2
diff -u -r1.2 xeview.cxx
--- sc/source/filter/excel/xeview.cxx	21 Feb 2005 13:31:18 -0000	1.2
+++ sc/source/filter/excel/xeview.cxx	21 Mar 2005 15:30:15 -0000
@@ -397,7 +397,8 @@
 
 void XclExpTabViewSettings::WriteWindow2( XclExpStream& rStrm ) const
 {
-    XclExpWindow2( GetRoot(), maData, mnGridColorId ).Save( rStrm );
+	const XclExpRoot& rRoot = GetRoot();
+    XclExpWindow2( rRoot, maData, mnGridColorId ).Save( rStrm );
 }
 
 void XclExpTabViewSettings::WriteScl( XclExpStream& rStrm ) const
@@ -409,7 +410,10 @@
 void XclExpTabViewSettings::WritePane( XclExpStream& rStrm ) const
 {
     if( maData.IsSplit() )
-        XclExpPane( GetRoot(), maData ).Save( rStrm );
+	{
+		const XclExpRoot& rRoot = GetRoot();
+        XclExpPane( rRoot, maData ).Save( rStrm );
+	}
 }
 
 void XclExpTabViewSettings::WriteSelection( XclExpStream& rStrm, sal_uInt8 nPane ) const
