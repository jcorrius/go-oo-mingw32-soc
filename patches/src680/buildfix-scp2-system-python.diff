Index: scp2/source/python/file_python.scp
===================================================================
RCS file: /cvs/installation/scp2/source/python/file_python.scp,v
retrieving revision 1.4
diff -u -p -u -r1.4 file_python.scp
--- scp2/source/python/file_python.scp	5 Jul 2004 13:19:01 -0000	1.4
+++ scp2/source/python/file_python.scp	17 Nov 2004 17:34:46 -0000
@@ -105,12 +105,14 @@ File gid_File_Py_Pythonloader
     Styles = (PACKED);
 End
 
+#ifndef SYSTEM_PYTHON
 File gid_File_Py_Python_Core
     TXT_FILE_BODY;
     Dir = gid_Dir_Program;
     Name = "python-core-2.2.2.zip";
     Styles = (ARCHIVE);
 End
+#endif
 
 // Items for Python Scripting Framework Runtime 
 // Scripting Framework ScriptProvider for Python
@@ -180,6 +182,7 @@ End
 
 #ifdef UNX
 #ifndef MACOSX
+#ifndef SYSTEM_PYTHON
 
 File gid_File_Lib_Python_So
     TXT_FILE_BODY;
@@ -188,12 +191,6 @@ File gid_File_Lib_Python_So
     Styles = (PACKED);
 End
 
-#endif
-#endif
-
-#ifdef UNX
-#ifndef MACOSX
-
 Shortcut gid_Shortcut_Lib_Python_1
     FileID = gid_File_Lib_Python_So;
     Dir = gid_Dir_Program;
@@ -203,9 +200,11 @@ End
 
 #endif
 #endif
+#endif
 
 #ifdef UNX
 #ifdef MACOSX
+#ifndef SYSTEM_PYTHON
 
 File gid_File_Lib_Python_So
     TXT_FILE_BODY;
@@ -214,12 +213,6 @@ File gid_File_Lib_Python_So
     Styles = (PACKED);
 End
 
-#endif
-#endif
-
-#ifdef UNX
-#ifdef MACOSX
-
 Shortcut gid_Shortcut_Lib_Python_1
     FileID = gid_File_Lib_Python_So;
     Dir = gid_Dir_Program;
@@ -229,8 +222,10 @@ End
 
 #endif
 #endif
+#endif
 
 #ifdef UNX
+#ifndef SYSTEM_PYTHON
 
 Shortcut gid_Shortcut_Lib_Python_2
     FileID = gid_File_Lib_Python_So;
@@ -240,6 +235,7 @@ Shortcut gid_Shortcut_Lib_Python_2
 End
 
 #endif
+#endif
 
 #ifdef UNX
 
@@ -254,11 +250,13 @@ End
 
 #ifdef UNX
 
+#ifndef SYSTEM_PYTHON
 File gid_File_Python_Sh
     BIN_FILE_BODY;
     Name = "python.sh";
     Dir = gid_Dir_Program;
     Styles = (PACKED);
 End
+#endif
 
 #endif
Index: scp2/source/python/makefile.mk
===================================================================
RCS file: /cvs/installation/scp2/source/python/makefile.mk,v
retrieving revision 1.5
diff -u -p -u -r1.5 makefile.mk
--- scp2/source/python/makefile.mk	5 Jul 2004 13:19:17 -0000	1.5
+++ scp2/source/python/makefile.mk	17 Nov 2004 17:34:46 -0000
@@ -72,6 +72,10 @@ TARGETTYPE=CUI
 
 .INCLUDE :	settings.mk
 
+.IF "$(SYSTEM_PYTHON)" == "YES"
+SCPDEFS+=-DSYSTEM_PYTHON
+.ENDIF
+
 SCP_PRODUCT_TYPE=osl
 
 PARFILES=\
