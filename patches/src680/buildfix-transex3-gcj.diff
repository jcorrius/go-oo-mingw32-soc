--- transex3/java/l10nconv/java/com/sun/star/tooling/converter/GSIReader.java	2007-05-11 11:09:13.000000000 +0200
+++ transex3/java/l10nconv/java/com/sun/star/tooling/converter/GSIReader.java	2007-06-21 18:10:30.000000000 +0200
@@ -61,7 +61,7 @@ public class GSIReader extends DataReade
     /**
      * @see LanguageResolver
      */
-    LanguageResolver            languageResolver = new LanguageResolver();
+    LanguageResolver            languageResolver;
 
     /**
      * The source language identifier
@@ -127,6 +127,7 @@ public class GSIReader extends DataReade
         super(new InputStreamReader(new FileInputStream(source), charset));
         this.sourceLanguage = sourceLanguage;
         this.targetLanguage = targetLanguage;
+        this.languageResolver = new LanguageResolver();
 
         EmptyLanguageMap = new ExtMap(new String[0], new String[0]);
 
--- transex3/java/l10nconv/java/com/sun/star/tooling/converter/XLIFFReader.java	2007-06-21 17:58:27.000000000 +0200
+++ transex3/java/l10nconv/java/com/sun/star/tooling/converter/XLIFFReader.java	2007-06-21 18:10:21.000000000 +0200
@@ -281,7 +281,7 @@ public class XLIFFReader extends Default
     /**
      * used to find the matching ISO or RFC3066 language code
      */
-    LanguageResolver languageResolver= new LanguageResolver();
+    LanguageResolver languageResolver;
 
     private boolean doBlockCompleteCheck=true;
 
@@ -298,6 +298,7 @@ public class XLIFFReader extends Default
         
         this.handler = handler;
         this.target = target;
+        this.languageResolver = new LanguageResolver();
     }
     
     /**
@@ -714,7 +714,7 @@ public class XLIFFReader extends Default
         //create the new 'id'
         sParts = data[BLOCKNR_IDX].substring(data[BLOCKNR_IDX].lastIndexOf(":") + 1);
 
-//GH das wird doch durch die nächste Abfrage mit erschlagen oder?
+//GH das wird doch durch die naechste Abfrage mit erschlagen oder?
 // jo
 //        if ("1".equals(sParts)) {
 //            return true;
--- transex3/java/l10nconv/java/com/sun/star/tooling/converter/XLIFFWriter.java	2007-05-11 11:10:59.000000000 +0200
+++ transex3/java/l10nconv/java/com/sun/star/tooling/converter/XLIFFWriter.java	2007-06-21 18:11:28.000000000 +0200
@@ -89,7 +89,7 @@ public class XLIFFWriter extends DataWri
 	 */
 	boolean isFirst = true;
     
-    LanguageResolver languageResolver=new LanguageResolver();
+    LanguageResolver languageResolver;
 
 	
 	/**
@@ -103,8 +103,7 @@ public class XLIFFWriter extends DataWri
 			throws IOException {
 		super(bos, charset);
         
-        
-		
+		this.languageResolver = new LanguageResolver();
 	}
 
 	/* (non-Javadoc)
@@ -334,7 +333,7 @@ public class XLIFFWriter extends DataWri
     		// //sLineNumber=String.valueOf(iLineNumber);//
     		// writeBuffer.append("\t\t<trans-unit
     		// id=\""+this.data.get("BlockNr")+":"+parts+"\"
-    		// restype=\""+sRessource+"\" translate=\"yes\">\n");//Immer übersetzen
+    		// restype=\""+sRessource+"\" translate=\"yes\">\n");//Immer uebersetzen
     		// if(isUsed((String)this.data.get("SourceHText")))
     		// writeBuffer.append("\t\t\t<source
     		// xml:lang=\""+this.data.get("SourceLanguageID")+"\">"+xmlString((String)this.data.get("SourceHText"))+"</source>\n");
@@ -352,7 +351,7 @@ public class XLIFFWriter extends DataWri
     			writeBuffer.append("\t\t\t<trans-unit id=\""
     					+ this.data.get("BlockNr") + ":" + parts + "\" restype=\""
     					+ sRessource + "\" translate=\"yes\">\n");// Immer
-    																// übersetzen
+    																// uebersetzen
     			if (isUsed((String) this.data.get("SourceQText")))
     				writeBuffer.append("\t\t\t\t<source xml:lang=\""
     						+ languageResolver.getRFCFromISO((String)this.data.get("SourceLanguageID")) + "\">"
@@ -376,7 +375,7 @@ public class XLIFFWriter extends DataWri
     			writeBuffer.append("\t\t\t<trans-unit id=\""
     					+ this.data.get("BlockNr") + ":" + parts + "\" restype=\""
     					+ sRessource + "\" translate=\"yes\">\n");// Immer
-    																// übersetzen
+    																// uebersetzen
     			if (isUsed((String) this.data.get("SourceTitle")))
     				writeBuffer.append("\t\t\t\t<source xml:lang=\""
     						+ languageResolver.getRFCFromISO((String)this.data.get("SourceLanguageID")) + "\">"
@@ -539,4 +538,4 @@ public class XLIFFWriter extends DataWri
 	 */
 	protected void getDataFrom(DataHandler handler) {	}
 
-}
\ Chybi znak konce radku na konci souboru
+}
