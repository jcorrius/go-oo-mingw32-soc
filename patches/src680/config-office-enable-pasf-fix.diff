Index: configure.in
===================================================================
RCS file: /cvs/tools/config_office/configure.in,v
retrieving revision 1.101
diff -u -u -r1.101 configure.in
--- config_office/configure.in	1 Mar 2005 09:38:16 -0000	1.101
+++ config_office/configure.in	16 Mar 2005 02:28:31 -0000
@@ -2349,6 +2349,7 @@
 AC_MSG_CHECKING([whether to enable portaudio/sndfile support])
 if test -n "$enable_pasf"; then
 	AC_MSG_RESULT([yes])
+   ENABLE_PASF=YES
   	AC_MSG_CHECKING([which sndfile to use])
 	if test -n "$with_system_sndfile" -o -n "$with_system_libs" && \
 		test "$with_system_sndfile" != "no"; then
