--- xmlsecurity/util/makefile.mk.orig	2006-01-29 03:29:38.000000000 +0100
+++ xmlsecurity/util/makefile.mk	2006-01-29 03:32:25.000000000 +0100
@@ -110,8 +110,12 @@
 
 # workaround for xmlsec1.2.9, -nss uses -openssl funktions without really
 # depending on it
-.IF "$(SYSTEM_XMLSEC)" == "YES"
-SHL2STDLIBS += -lxmlsec1-openssl
+#.IF "$(SYSTEM_XMLSEC)" == "YES"
+#SHL2STDLIBS += -lxmlsec1-openssl
+#.ENDIF
+
+.IF "$(SYSTEM_MOZILLA)" == "YES"
+SHL2STDLIBS += -L/usr/lib/nss -L/usr/lib/nspr
 .ENDIF
 
 .IF "$(CRYPTO_ENGINE)" == "mscrypto"
