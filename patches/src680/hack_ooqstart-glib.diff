--- config_office/configure.in.orig	2007-01-13 15:08:30.000000000 +0100
+++ config_office/configure.in	2007-01-13 16:21:40.000000000 +0100
@@ -4944,6 +4944,9 @@
       fi
    fi
 
+dnl temporary hack: wrapper ooqstart needs glib
+PKG_CHECK_MODULES(GLIB, glib-2.0)
+
 fi
 AC_SUBST(ENABLE_SYSTRAY_GTK)
 AC_SUBST(GTK_CFLAGS)

--- desktop/unx/source/makefile.mk.orig	2007-01-13 15:10:20.000000000 +0100
+++ desktop/unx/source/makefile.mk	2007-01-13 15:10:38.000000000 +0100
@@ -6,12 +6,6 @@
 
 .INCLUDE :  settings.mk
 
-.IF "$(ENABLE_GTK)" != "TRUE"
-dummy:
-	@echo "Nothing to build. GUIBASE == $(GUIBASE)"
-
-.ELSE # we need glib...
-
 PKGCONFIG_MODULES=glib-2.0
 .INCLUDE: pkg_config.mk
 
@@ -29,8 +23,6 @@
 APP1CODETYPE = C
 APP1STDLIBS = $(PKGCONFIG_LIBS) -lX11 -lsal$(DLLPOSTFIX)
 
-.ENDIF
-
 # --- Targets ------------------------------------------------------
 
 .INCLUDE :	target.mk

--- scp2/source/ooo/file_ooo.scp.orig	2007-01-13 17:31:14.000000000 +0100
+++ scp2/source/ooo/file_ooo.scp	2007-01-13 17:31:28.000000000 +0100
@@ -330,7 +330,7 @@
     Name = "soffice.bin";
 End
 
-#if defined(UNX) && defined(ENABLE_GTK)
+#if defined(UNX)
 File gid_File_Bin_QStart_Bin
     BIN_FILE_BODY;
     Dir = gid_Dir_Program;
