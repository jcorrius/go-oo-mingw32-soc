Index: instsetoo_native/util/makefile.mk
===================================================================
--- instsetoo_native/util/makefile.mk.orig	2005-05-05 11:57:16.000000000 +0000
+++ instsetoo_native/util/makefile.mk	2005-05-06 11:15:37.750470504 +0000
@@ -92,6 +92,12 @@
 	$(BIN)$/pythonloader.py \
 	$(BIN)$/pythonscript.py
 
+.IF "$(SYSTEM_PYTHON)" != "YES"
+LOCALPYFILES+= \
+	$(BIN)$/cp1251.py
+.ENDIF
+
+
 # PKGFORMAT taken from environment. See possible
 # values below.
 #
@@ -165,14 +171,16 @@
 .ENDIF			# "$(alllangiso)"!=""
 
 .IF "$(LOCALPYFILES)"!=""
-$(foreach,i,$(alllangiso) openoffice_$i) updatepack $(foreach,i,$(alllangiso) ooolanguagepack_$i) : $(LOCALPYFILES) $(BIN)$/cp1251.py
+$(foreach,i,$(alllangiso) openoffice_$i) updatepack $(foreach,i,$(alllangiso) ooolanguagepack_$i) : $(LOCALPYFILES)
 .ENDIF			# "$(LOCALPYFILES)"!=""
 
 $(BIN)$/%.py : $(SOLARSHAREDBIN)$/pyuno$/%.py
 	@+$(COPY) $< $@
 
+.IF "$(SYSTEM_PYTHON)" != "YES"
 $(BIN)$/cp1251.py : $(SOLARLIBDIR)$/python$/encodings$/cp1251.py
 	@+$(COPY) $< $@
+.ENDIF
 
 $(BIN)$/intro.bmp : $(SOLARCOMMONPCKDIR)$/openoffice$/nologointro.bmp
 	+$(COPY) $< $@
