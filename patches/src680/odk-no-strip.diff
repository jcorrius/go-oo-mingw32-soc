Index: odk_rules.pmk
===================================================================
RCS file: /cvs/api/odk/util/odk_rules.pmk,v
retrieving revision 1.21
diff -u -u -r1.21 odk_rules.pmk
--- odk/util/odk_rules.pmk	3 Aug 2006 15:12:25 -0000	1.21
+++ odk/util/odk_rules.pmk	28 Sep 2006 14:15:08 -0000
@@ -121,14 +121,12 @@
 	+-rm -f $@ >& $(NULLDEV)
 	$(GNUCOPY) $(BINOUT)$/$(@:f) $@
 .IF "$(GUI)"=="UNX"
-	+-strip $@
 .ENDIF
 
 $(DESTDIRBIN)$/%.bin : $(BINOUT)$/%.bin
 	+-rm -f $@ >& $(NULLDEV)
 	$(GNUCOPY) $(BINOUT)$/$(@:f) $@
 .IF "$(GUI)"=="UNX"
-	+-strip $@
 .ENDIF
 
 $(DESTDIRBIN)$/regcomp : $(BINOUT)$/regcomp
@@ -142,7 +140,6 @@
 	+-rm -f $@ >& $(NULLDEV)
 	$(GNUCOPY) $(BIN)$/$(@:f) $@
 .IF "$(GUI)"=="UNX"
-	+-strip $@
 .ENDIF
 
 # special rule for special Java UNO native library
