--- forms/source/misc/InterfaceContainer.cxx	12 Oct 2006 11:14:25 -0000	1.26
+++ forms/source/misc/InterfaceContainer.cxx	13 Nov 2006 09:10:25 -0000	1.26.4.1
@@ -564,6 +564,9 @@
 
 	if ( m_aItems.end() != j )
 	{
+        m_aItems.erase(j);
+
+        // look up in, and erase from, m_aMap, too
 		OInterfaceMap::iterator i = m_aMap.begin();
 		while ( i != m_aMap.end() )
 		{
@@ -571,14 +574,16 @@
 				"OInterfaceContainer::disposing: map element not normalized!" );
 
 			if ( i->second.get() == _rSource.Source.get() )
+            {
 				// found it
+                m_aMap.erase(i);
 				break;
+            }
 
 			++i;
-		}
 
-		m_aMap.erase(i);
-		m_aItems.erase(j);
+            DBG_ASSERT( i != m_aMap.end(), "OInterfaceContainer::disposing: inconsistency: the element was in m_aItems, but not in m_aMap!" );
+		}
 	}
 }
 
