--- forms/source/misc/InterfaceContainer.cxx.orig	2006-11-05 20:23:12.000000000 +0100
+++ forms/source/misc/InterfaceContainer.cxx	2006-11-05 20:24:02.000000000 +0100
@@ -571,14 +571,14 @@
 				"OInterfaceContainer::disposing: map element not normalized!" );
 
 			if ( i->second.get() == _rSource.Source.get() )
-				// found it
+			{	// found it
+				m_aMap.erase(i);
+				m_aItems.erase(j);
 				break;
+			}
 
 			++i;
 		}
-
-		m_aMap.erase(i);
-		m_aItems.erase(j);
 	}
 }
 
