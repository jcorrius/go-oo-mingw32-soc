--- vcl/source/glyphs/gcach_ftyp.cxx	2006-11-01 10:12:01.000000000 +0800
+++ vcl/source/glyphs/gcach_ftyp.cxx	2006-11-01 10:22:21.000000000 +0800
@@ -1268,8 +1268,6 @@ void FreetypeServerFont::InitGlyphData( 
     }
     rGD.SetCharWidth( (nCharWidth + 32) >> 6 );
 
-    if( mbArtBold && pFTEmbolden )
-        (*pFTEmbolden)( maFaceFT->glyph );
     FT_Glyph pGlyphFT;
     rc = FT_Get_Glyph( maFaceFT->glyph, &pGlyphFT );
 
@@ -1343,9 +1341,6 @@ bool FreetypeServerFont::GetGlyphBitmap1
     if( rc != FT_Err_Ok )
         return false;
 
-    if( mbArtBold && pFTEmbolden )
-        (*pFTEmbolden)( maFaceFT->glyph );
-
     FT_Glyph pGlyphFT;
     rc = FT_Get_Glyph( maFaceFT->glyph, &pGlyphFT );
     if( rc != FT_Err_Ok )
@@ -2166,8 +2161,6 @@ bool FreetypeServerFont::GetGlyphOutline
     if( rc != FT_Err_Ok )
         return false;
 
-    if( mbArtBold && pFTEmbolden )
-        (*pFTEmbolden)( maFaceFT->glyph );
 
     FT_Glyph pGlyphFT;
     rc = FT_Get_Glyph( maFaceFT->glyph, &pGlyphFT );
