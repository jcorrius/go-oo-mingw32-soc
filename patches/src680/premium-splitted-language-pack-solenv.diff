--- solenv/bin/modules/installer/languagepack.pm.orig	2007-08-10 19:16:31.000000000 +0200
+++ solenv/bin/modules/installer/languagepack.pm	2007-09-12 15:16:21.739698400 +0200
@@ -94,6 +94,8 @@
 						if ( $oneitem->{'Dir'} eq "gid_Dir_Fonts_Truetype" ) { $oneitem->{'modules'} = "gid_Module_Langpack_Fonts"; }
 						if ( $oneitem->{'Dir'} eq "gid_Dir_Resource" ) { $oneitem->{'modules'} = "gid_Module_Langpack_Resource"; }
 						if ( $oneitem->{'Dir'} eq "gid_Dir_Help_Isolanguage" ) { $oneitem->{'modules'} = "gid_Module_Langpack_Help"; }
+						if ( $oneitem->{'Dir'} eq "gid_Dir_Samples_Language" ) { $oneitem->{'modules'} = "gid_Module_Langpack_Accessories_Samples"; }
+						if ( $oneitem->{'Dir'} eq "gid_Dir_Template_Language" ) { $oneitem->{'modules'} = "gid_Module_Langpack_Accessories_Templates"; }
 					}
 				}
 
@@ -200,7 +202,8 @@
 		if ( ${$alldirs}[$i] =~ /-fonts/ ) { next; } 
 		if ( ${$alldirs}[$i] =~ /-help/ ) { next; } 
 		if ( ${$alldirs}[$i] =~ /-res/ ) { next; } 
-	
+        	if ( ${$alldirs}[$i] =~ /-premium-samples/ ) { next; }
+		if ( ${$alldirs}[$i] =~ /-premium-templates/ ) { next; }
 		$packagename = ${$alldirs}[$i];
 		last;
 	}
