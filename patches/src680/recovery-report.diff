Index: svx/source/dialog/docrecovery.src
===================================================================
RCS file: /cvs/graphics/svx/source/dialog/docrecovery.src,v
retrieving revision 1.3
diff -u -r1.3 docrecovery.src
--- svx/source/dialog/docrecovery.src	9 Dec 2004 16:44:31 -0000	1.3
+++ svx/source/dialog/docrecovery.src	15 Dec 2004 17:43:32 -0000
@@ -192,8 +192,8 @@
 		Pos = MAP_APPFONT( RECOV_COL1, RECOV_ROW2 );
         Size = MAP_APPFONT( RECOV_CONTROLWIDTH, RECOV_DESCRHEIGHT );
 		WordBreak = TRUE;
-        Text[ de ] = "%PRODUCTNAME stellt jetzt Ihre Dokumente wieder her. Abhängig von der Größe der Dokumente kann dieser Vorgang etwas länger dauern.\n\nBitte unterstützen Sie uns Fehler in %PRODUCTNAME zu beheben. Von dem Absturz wurde ein Bericht erstellt der uns hilft diesen Fehler zu beseitigen. Klicken Sie \'Weiter\' um zu den Fehlerberichtassistenten zu gelangen.";
-		Text[ en-US ] = "%PRODUCTNAME starts now to recover your documents. Depending on the size of the documents this process can take some time.\n\nPlease support us to solve the issue why %PRODUCTNAME crashed. Click \'Next\' to get to the Error Report Tool.";
+        Text[ de ] = "%PRODUCTNAME stellt jetzt Ihre Dokumente wieder her. Abhängig von der Größe der Dokumente kann dieser Vorgang etwas länger dauern.\n\n";
+		Text[ en-US ] = "%PRODUCTNAME starts now to recover your documents. Depending on the size of the documents this process can take some time.\n\n";
 	};
 	FixedText FT_RECOV_PROGR
 	{
@@ -244,8 +244,8 @@
 	{
 		Pos = MAP_APPFONT( RECOV_COL_D, RECOV_ROW9 );
 		Size = MAP_APPFONT( RSC_CD_PUSHBUTTON_WIDTH, RSC_CD_PUSHBUTTON_HEIGHT );
-		Text[ de ] = "Weiter>>";
-		Text[ en-US ] = "Next>>";
+		Text [ en-US ] = "~Finish";
+		Text [ de ]    = "~Fertig";
         DefButton = TRUE;
 	};
 	CancelButton BTN_RECOV_CANCEL
Index: svx/source/unodraw/recoveryui.cxx
===================================================================
RCS file: /cvs/graphics/svx/source/unodraw/recoveryui.cxx,v
retrieving revision 1.2
diff -u -r1.2 recoveryui.cxx
--- svx/source/unodraw/recoveryui.cxx	26 Nov 2004 14:22:49 -0000	1.2
+++ svx/source/unodraw/recoveryui.cxx	15 Dec 2004 17:43:46 -0000
@@ -343,19 +343,23 @@
     // and bind it to the used core service
     svxdr::TabDialog4Recovery* pWizard = new svxdr::TabDialog4Recovery   (m_pParentWindow);
     svxdr::IExtendedTabPage*   pPage1  = new svxdr::RecoveryDialog       (pWizard, pCore );
+    pWizard->addTabPage(pPage1);
+#ifdef SUN_CRASH_REPORT
     svxdr::IExtendedTabPage*   pPage2  = new svxdr::ErrorRepWelcomeDialog(pWizard        );
     svxdr::IExtendedTabPage*   pPage3  = new svxdr::ErrorRepSendDialog   (pWizard        );
-    pWizard->addTabPage(pPage1);
     pWizard->addTabPage(pPage2);
     pWizard->addTabPage(pPage3);
+#endif
     
     // start the wizard
     short nRet = pWizard->Execute();
 
     impl_showAllRecoveredDocs();
     
+#ifdef SUN_CRASH_REPORT
     delete pPage3 ;
     delete pPage2 ;
+#endif
     delete pPage1 ;
     delete pWizard;
 
@@ -369,16 +373,20 @@
 	if ( new_crash_pending() )
 	{
 		svxdr::TabDialog4Recovery* pWizard = new svxdr::TabDialog4Recovery   (m_pParentWindow   );
+#ifdef SUN_CRASH_REPORT
 		svxdr::IExtendedTabPage*   pPage1  = new svxdr::ErrorRepWelcomeDialog(pWizard, sal_False);
 		svxdr::IExtendedTabPage*   pPage2  = new svxdr::ErrorRepSendDialog   (pWizard           );
 		pWizard->addTabPage(pPage1);
 		pWizard->addTabPage(pPage2);
+#endif
 	    
 		// start the wizard
 		short nRet = pWizard->Execute();
 	    
+#ifdef SUN_CRASH_REPORT
 		delete pPage2 ;
 		delete pPage1 ;
+#endif
 		delete pWizard;
 
 		delete_pending_crash();
