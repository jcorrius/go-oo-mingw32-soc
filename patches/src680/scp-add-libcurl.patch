--- scp2/source/ooo/file_library_ooo.scp	2005-01-31 20:05:08.000000000 +0100
+++ scp2/source/ooo/file_library_ooo.scp	2005-02-01 17:13:12.073600157 +0100
@@ -416,7 +416,7 @@
     Styles = (PACKED);
     Dir = gid_Dir_Program;
   #ifdef UNX
-    Name = STRING(CONCAT3(libcurl,UNXSUFFIX,.3));
+    Name = STRING(CONCAT8(libcurl,UNXSUFFIX,.,CURL_MAJOR,.,CURL_MINOR,.,CURL_MICRO));
   #else
     Name = "libcurl.dll";
   #endif
--- scp2/inc/macros.inc	2005-01-13 20:19:23.000000000 +0100
+++ scp2/inc/macros.inc	2005-02-01 17:07:37.214479214 +0100
@@ -28,6 +28,10 @@
 #define ICU_MINOR 6
 #define ICU_MICRO 0
 
+#define CURL_MAJOR 3
+#define CURL_MINOR 0
+#define CURL_MICRO 0
+
 #define MOD_NAME_DESC(id) \
         ALL_LANG(Name,STR_NAME_##id); \
         ALL_LANG(Description,STR_DESC_##id)
--- scp2/source/ooo/shortcut_ooo.scp	2004-12-16 11:53:07.000000000 +0100
+++ scp2/source/ooo/shortcut_ooo.scp	2005-02-01 17:22:14.404842662 +0100
@@ -162,6 +162,32 @@
 
 #endif
 
+#ifndef SYSTEM_CURL
+
+#ifdef UNX
+
+Shortcut gid_Shortcut_Lib_Curl_0
+    FileID = gid_File_Lib_Curl;
+    Dir = gid_Dir_Program;
+    Name = STRING(CONCAT2(libcurl,UNXSUFFIX));
+    Styles = (NETWORK,RELATIVE);
+End
+
+#endif
+
+#ifdef UNX
+
+Shortcut gid_Shortcut_Lib_Curl_1
+    FileID = gid_File_Lib_Curl;
+    Dir = gid_Dir_Program;
+    Name = STRING(CONCAT4(libcurl,UNXSUFFIX,.,CURL_MAJOR));
+    Styles = (NETWORK,RELATIVE);
+End
+
+#endif
+
+#endif
+
 #ifdef UNX
 
 Shortcut gid_Shortcut_Lib_Icudata_0
