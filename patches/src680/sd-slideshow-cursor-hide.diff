Index: sd/source/ui/inc/ShowWindow.hxx
===================================================================
RCS file: /cvs/graphics/sd/source/ui/inc/ShowWindow.hxx,v
retrieving revision 1.6
diff -u -p -u -r1.6 ShowWindow.hxx
--- sd/source/ui/inc/ShowWindow.hxx	9 Sep 2005 05:14:40 -0000	1.6
+++ sd/source/ui/inc/ShowWindow.hxx	25 Jan 2006 11:42:02 -0000
@@ -120,6 +120,7 @@ private:
 
 private:
 	Timer			maPauseTimer;
+	Timer			maMouseTimer;
 	Wallpaper		maShowBackground;
 	Graphic			maLogo;
 	ULONG			mnPauseTimeout;
@@ -130,6 +131,8 @@ private:
 
 	sd::Slideshow*	mpSlideshow;
 					DECL_LINK( PauseTimeoutHdl, Timer* pTimer );
+					DECL_LINK( MouseTimeoutHdl, Timer* pTimer );
+					DECL_LINK( EventHdl, VclWindowEvent* pEvent );
 };
 
 } // end of namespace sd
Index: sd/source/ui/slideshow/showwin.cxx
===================================================================
RCS file: /cvs/graphics/sd/source/ui/slideshow/showwin.cxx,v
retrieving revision 1.8
diff -u -p -u -r1.8 showwin.cxx
--- sd/source/ui/slideshow/showwin.cxx	19 Oct 2005 12:25:16 -0000	1.8
+++ sd/source/ui/slideshow/showwin.cxx	25 Jan 2006 11:42:02 -0000
@@ -92,15 +92,20 @@ ShowWindow::ShowWindow( ::Window* pParen
 
 	maPauseTimer.SetTimeoutHdl( LINK( this, ShowWindow, PauseTimeoutHdl ) );
 	maPauseTimer.SetTimeout( 1000 );
+	maMouseTimer.SetTimeoutHdl( LINK( this, ShowWindow, MouseTimeoutHdl ) );
+	maMouseTimer.SetTimeout( 2000 );
 
 	maShowBackground = Wallpaper( Color( COL_BLACK ) );
 //	SetBackground( Wallpaper( Color( COL_BLACK ) ) );
     SetBackground(); // avoids that VCL paints any background!
 	GetParent()->Show();
+	AddEventListener( LINK( this, ShowWindow, EventHdl ) );
 }
 
 ShowWindow::~ShowWindow(void)
 {
+	maPauseTimer.Stop();
+	maMouseTimer.Stop();
 }
 
 /*************************************************************************
@@ -211,6 +216,9 @@ void ShowWindow::MouseButtonDown(const M
 
 void ShowWindow::MouseMove(const MouseEvent& rMEvt)
 {
+	ShowPointer( TRUE );
+	maMouseTimer.Start();
+
 	if( mpViewShell )
 	{
 		if( SHOWWINDOWMODE_NORMAL == meShowWindowMode )
@@ -323,6 +331,7 @@ long ShowWindow::Notify(NotifyEvent& rNE
 		nOK = mpViewShell->GetViewFrame()->GetWindow().Notify(aNEvt);
 	}
 */
+
 	if (!nOK)
 		nOK = Window::Notify(rNEvt);
 
@@ -470,6 +479,7 @@ void ShowWindow::TerminateShow()
 {
 	maLogo.Clear();
 	maPauseTimer.Stop();
+	maMouseTimer.Stop();
 	Erase();
 //	SetBackground( maShowBackground );
 	maShowBackground = Wallpaper( Color( COL_BLACK ) );
@@ -661,6 +671,18 @@ IMPL_LINK( ShowWindow, PauseTimeoutHdl, 
 	return 0L;
 }
 
+IMPL_LINK( ShowWindow, MouseTimeoutHdl, Timer*, pTimer )
+{
+	ShowPointer( FALSE );
+	return 0L;
+}
+
+IMPL_LINK( ShowWindow, EventHdl, VclWindowEvent*, pEvent )
+{
+	if (pEvent->GetId() == VCLEVENT_WINDOW_SHOW)
+		maMouseTimer.Start();
+	return 0L;
+}
 
 void ShowWindow::SetPresentationArea( const Rectangle& rPresArea )
 {
