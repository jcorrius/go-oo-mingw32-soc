--- desktop/unx/source/makefile.mk	2006-06-22 11:59:08.000000000 +0000
+++ desktop/unx/source/makefile.mk	2006-06-22 12:00:46.000000000 +0000
@@ -15,7 +15,8 @@ dummy:
 PKGCONFIG_MODULES=glib-2.0
 .INCLUDE: pkg_config.mk
 
-CFLAGS+= $(PKGCONFIG_CFLAGS)
+CFLAGS += $(PKGCONFIG_CFLAGS)
+CFLAGS += -DINTRO_BITMAP_NAMES=\"$(INTRO_BITMAP_NAMES)\"
 STDLIB=
 
 APP1TARGET = $(TARGET)
--- desktop/unx/source/start.c	2006-06-22 11:58:41.000000000 +0000
+++ desktop/unx/source/start.c	2006-06-22 12:16:29.000000000 +0000
@@ -231,17 +231,28 @@ status_pipe_update (GIOChannel * source,
 static void
 load_splash_image (const char *image_path)
 {
-	char *path;
-	int success;
+	char *path, *filename;
+	int success = 0;
+	char **bitmap_names, **iter;
 
-	path = g_strconcat (image_path, "/intro-nld.bmp", NULL);
-	success = splash_load_bmp (path);
-	g_free (path);
-	if (success)
-		return;
+	bitmap_names = g_strsplit (INTRO_BITMAP_NAMES, ",", 0);
+	path = g_strconcat (image_path, "/", NULL);
+
+	for (iter = bitmap_names; *iter != NULL && !success; ++iter)
+	{
+		filename = g_strconcat (path, *iter, NULL);
+		success = splash_load_bmp (filename);
+		g_free (filename);
+	}
+	g_strfreev (bitmap_names);
+	
+	if (!success)
+	{
+		filename = g_strconcat (path, "intro.bmp", NULL);
+		success = splash_load_bmp (filename);
+		g_free (filename);
+	}
 
-	path = g_strconcat (image_path, "/intro.bmp", NULL);
-	success = splash_load_bmp (path);
 	g_free (path);
 }
 
