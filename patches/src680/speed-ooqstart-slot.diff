--- desktop/unx/source/start.c	2006-03-13 15:32:46.000000000 +0000
+++ desktop/unx/source/start.c	2006-03-13 15:32:42.000000000 +0000
@@ -367,6 +367,9 @@ convert_path (const char *arg, const cha
 	if (arg[0] == '/') /* absolute path */
 		return g_strdup (arg);
 
+	if (!strncmp (arg, "slot:", 5)) /* a slot */
+		return g_strdup (arg);
+
 	/* prepend cwd ... */
 	return g_strconcat (cwd_str, "/", arg, NULL);
 }
