--- framework/source/helper/statusindicatorfactory.cxx	2003-03-25 18:21:40.000000000 +0000
+++ framework/source/helper/statusindicatorfactory.cxx	2004-08-05 21:59:50.880726232 +0100
@@ -253,7 +253,7 @@
     m_aStack.push_back (aInfo                );
 
     m_xActiveChild = xChild;
-    m_nStartTime   = impl_get10ThSec();
+    m_nLastReschedule = m_nStartTime = impl_get10ThSec();
     
     css::uno::Reference< css::task::XStatusIndicator > xProgress = m_xProgress;
     
@@ -402,7 +402,10 @@
 
 	// We start rescheduling only after 1 second - this code was successfully introduced by the sfx2
 	// implementation of the progress bar.
-    sal_Bool bReschedule = ((impl_get10ThSec()-nStartTime ) > TIMEOUT_START_RESCHEDULE);
+    sal_Int32 nTime = impl_get10ThSec();
+    sal_Bool bReschedule = ( nTime != m_nLastReschedule &&
+                             (nTime - m_nStartTime ) > TIMEOUT_START_RESCHEDULE );
+    m_nLastReschedule = nTime;
 	if (bReschedule)
         impl_reschedule();
 }
--- framework/inc/helper/statusindicatorfactory.hxx	2003-03-25 18:19:43.000000000 +0000
+++ framework/inc/helper/statusindicatorfactory.hxx	2004-08-05 21:36:58.092223568 +0100
@@ -292,6 +292,9 @@
         /** time where there last start call was made. */
         sal_Int32 m_nStartTime;
 
+        /** time of our last reschedule call */
+        sal_Int32 m_nLastReschedule;
+
     //-------------------------------------------
     // interface
     
