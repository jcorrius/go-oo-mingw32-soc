--- sw/source/filter/ww8/writerwordglue.cxx	27 Sep 2007 10:01:10 -0000	1.25
+++ sw/source/filter/ww8/writerwordglue.cxx	11 Dec 2007 16:35:42 -0000
@@ -981,6 +981,15 @@
                     // Should be made with i18n framework.
                     switch ( rLang )
                     {
+					case LANGUAGE_DUTCH:
+					case LANGUAGE_DUTCH_BELGIAN:
+						{
+                            if (nChar == 'y' || nChar == 'Y')
+                                rParams.SetChar (nI, 'J');
+                            else if (nChar == 'u' || nChar == 'U')
+                                rParams.SetChar (nI, 'H');
+                        }
+                        break;
                     case LANGUAGE_GERMAN:
                         {
                             if (nChar == 'y' || nChar == 'Y')
