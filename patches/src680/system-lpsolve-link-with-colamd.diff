--- scsolver/util/makefile.mk-old	2006-06-20 14:25:21.000000000 +0200
+++ scsolver/util/makefile.mk	2006-06-20 14:27:13.000000000 +0200
@@ -29,6 +29,12 @@
 	$(TOOLSLIB) \
 	$(LPSOLVELIB)
 
+# DebianBaseOnly; we build lp_solve without colamd included but with system-colamd
+# (which is not linked into the .a...) so we need -lcolamd here, too
+.IF "$(SYSTEM_LPSOLVE)" == "YES"
+SHL1STDLIBS += -lcolamd
+.ENDIF
+
 RESLIB1NAME=scsolver
 RESLIB1SRSFILES= $(SRS)/scsolver.srs
 
