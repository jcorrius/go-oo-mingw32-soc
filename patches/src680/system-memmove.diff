--- soltools/cpp/_unix.c.old	2003-03-18 13:47:34.000000000 +0100
+++ soltools/cpp/_unix.c	2005-06-09 13:15:21.000000000 +0200
@@ -189,7 +189,7 @@
 /* memmove is defined here because some vendors don't provide it at
    all and others do a terrible job (like calling malloc) */
 
-#if !defined(__IBMC__) && !defined(_WIN32) 
+#if !defined(__IBMC__) && !defined(_WIN32) && !defined(__GLIBC__)
 
 void *
     memmove(void *dp, const void *sp, size_t n)
--- idlc/source/preproc/unix.c.old	2004-03-30 18:48:50.000000000 +0200
+++ idlc/source/preproc/unix.c	2005-06-09 18:24:41.000000000 +0200
@@ -244,7 +244,7 @@
 /* memmove is defined here because some vendors don't provide it at
    all and others do a terrible job (like calling malloc) */
 
-#if !defined(__IBMC__) && !defined(WNT)
+#if !defined(__IBMC__) && !defined(WNT) && !defined(__GLIBC__)
 
 void *
     memmove(void *dp, const void *sp, size_t n)
