--- vcl/util/makefile.mk	2004-10-12 17:02:18.514588312 +0200
+++ vcl/util/makefile.mk	2004-10-12 17:03:49.788712536 +0200
@@ -378,6 +378,19 @@ SHL4STDLIBS+=-l$(SHL2TARGET)
 SHL4STDLIBS+=$(SHL3STDLIBS) -lX11 -ldl
 .ENDIF # "$(ENABLE_GTK)" != ""
 
+# KDE plugin
+.IF "$(ENABLE_KDE)" != ""
+LIB5TARGET=$(SLB)$/ikde_plug_
+LIB5FILES=$(SLB)$/kdeplug.lib
+SHL5TARGET=vclplug_kde$(UPD)$(DLLPOSTFIX)
+SHL5IMPLIB=ikde_plug_
+SHL5LIBS=$(LIB5TARGET)
+# libs for KDE plugin
+SHL5STDLIBS=$(WIDGETSET_KDE_LIBS)
+SHL5STDLIBS+=-l$(SHL2TARGET)
+SHL5STDLIBS+=$(SHL3STDLIBS) -lX11 -ldl
+.ENDIF # "$(ENABLE_KDE)" != ""
+
 .ENDIF # UNX
 
 # --- Allgemein ----------------------------------------------------------
