--- nas/nas-1.6.patch.old	2007-01-15 18:17:51.000000000 +0100
+++ nas/nas-1.6.patch	2007-01-22 13:24:45.000000000 +0100
@@ -780,3 +780,26 @@
       XtAppAddActions(appContext, actions, XtNumber(actions));
   
       if (argc == 3)
+*** misc/nas-1.6/clients/audio/auedit/auedit.c.old	2007-01-15 18:34:12.000000000 +0100
+--- misc/build/nas-1.6/clients/audio/auedit/auedit.c	2007-01-22 13:17:43.000000000 +0100
+***************
+*** 1241,1248 ****
+      setUndo(g, left, left + numSamples, UNDO_REP);
+      p = g->data + left * g->numTracks;
+  
+!     while (numSamples--)
+! 	*p++ = (*p + *data++) / 2;
+  
+      if (newValues)
+  	XtVaSetValues(g->graph, XtNdata, g->data, XtNnumSamples, g->numSamples,
+--- 1241,1250 ----
+      setUndo(g, left, left + numSamples, UNDO_REP);
+      p = g->data + left * g->numTracks;
+  
+!     while (numSamples--) {
+! 	*p = (*p + *data++) / 2;
+! 	p++;
+!     }
+  
+      if (newValues)
+  	XtVaSetValues(g->graph, XtNdata, g->data, XtNnumSamples, g->numSamples,
