--- nas/nas-1.6.patch.old	2005-04-14 19:21:41.000000000 +0200
+++ nas/nas-1.6.patch	2005-04-19 15:21:43.000000000 +0200
@@ -603,3 +603,117 @@
   
   void * 
   debug_Xalloc (char *file, int line, unsigned long amount)
+*** misc/nas-1.6/clients/audio/audemo/audemo.c	2005-04-14 19:49:18.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/audemo/audemo.c	2005-04-19 15:19:51.000000000 +0200
+***************
+*** 1757,1763 ****
+  
+      globals->topLevel = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+  					  &argc, argv, defaultResources,
+! 					  NULL, 0);
+  
+      globals->volume = DEFAULT_VOLUME;
+      globals->numFiles = 0;
+--- 1757,1763 ----
+  
+      globals->topLevel = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+  					  &argc, argv, defaultResources,
+! 					  NULL);
+  
+      globals->volume = DEFAULT_VOLUME;
+      globals->numFiles = 0;
+*** misc/nas-1.6/clients/audio/auedit/auedit.c	2005-04-14 19:49:18.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/auedit/auedit.c	2005-04-19 15:18:40.000000000 +0200
+***************
+*** 2486,2492 ****
+      g = globals = &globalData;
+      r = &g->recordDialog;
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL, 0);
+      XtAppAddActions(appContext, actions, XtNumber(actions));
+  
+      while (--argc)
+--- 2486,2492 ----
+      g = globals = &globalData;
+      r = &g->recordDialog;
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL);
+      XtAppAddActions(appContext, actions, XtNumber(actions));
+  
+      while (--argc)
+*** misc/nas-1.6/clients/audio/aupanel/aupanel.c	2002-07-21 03:13:30.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/aupanel/aupanel.c	2005-04-19 15:11:28.000000000 +0200
+***************
+*** 418,424 ****
+      char           *audioServer = NULL;
+  
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL, 0);
+  
+      if (argc == 3)
+  	if (!strncmp(argv[1], "-a", 2))
+--- 418,424 ----
+      char           *audioServer = NULL;
+  
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL);
+  
+      if (argc == 3)
+  	if (!strncmp(argv[1], "-a", 2))
+*** misc/nas-1.6/clients/audio/auphone/auphone.c	2002-07-21 03:13:30.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/auphone/auphone.c	2005-04-19 15:19:05.000000000 +0200
+***************
+*** 1216,1222 ****
+      double          atof();
+  
+      g->top = XtVaAppInitialize(&g->appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL, 0);
+      XtAppAddActions(g->appContext, actions, XtNumber(actions));
+  
+      g->dpy = XtDisplay(g->top);
+--- 1216,1222 ----
+      double          atof();
+  
+      g->top = XtVaAppInitialize(&g->appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL);
+      XtAppAddActions(g->appContext, actions, XtNumber(actions));
+  
+      g->dpy = XtDisplay(g->top);
+*** misc/nas-1.6/clients/audio/autool/audiotool.c	2005-04-14 19:49:18.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/autool/audiotool.c	2005-04-19 15:18:09.000000000 +0200
+***************
+*** 481,487 ****
+  
+      globals->topLevel = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+  					  &argc, argv, defaultResources,
+! 					  NULL, 0);
+  
+      XtVaSetValues(globals->topLevel, XtNinput, True, NULL);
+  
+--- 481,487 ----
+  
+      globals->topLevel = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+  					  &argc, argv, defaultResources,
+! 					  NULL);
+  
+      XtVaSetValues(globals->topLevel, XtNinput, True, NULL);
+  
+*** misc/nas-1.6/clients/audio/auwave/auwave.c	2002-07-21 03:13:30.000000000 +0200
+--- misc/build/nas-1.6/clients/audio/auwave/auwave.c	2005-04-19 15:19:22.000000000 +0200
+***************
+*** 435,441 ****
+  
+      globals = g;
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL, 0);
+      XtAppAddActions(appContext, actions, XtNumber(actions));
+  
+      if (argc == 3)
+--- 435,441 ----
+  
+      globals = g;
+      g->top = XtVaAppInitialize(&appContext, APP_CLASS, NULL, ZERO,
+! 			       &argc, argv, defaultResources, NULL);
+      XtAppAddActions(appContext, actions, XtNumber(actions));
+  
+      if (argc == 3)
