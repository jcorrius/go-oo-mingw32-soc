--- binfilter/inc/bf_sfx2/urlframe.hxx.old	2006-10-28 05:11:20.000000000 +0200
+++ binfilter/inc/bf_sfx2/urlframe.hxx	2007-05-02 14:57:01.000000000 +0200
@@ -59,6 +59,8 @@ class SfxReleasableFrame;
 
 class SfxURLFrame : public SfxFrame
 {
+// this variable is only necessary to avoid a warning, see the method GetWindow
+Window *pFakeWindow;
 
 #if _SOLAR__PRIVATE
 #endif
@@ -78,7 +80,9 @@ public:
 										SfxReleasableFrame* pRel,
 										SfxFrame* pParent );
 
-							virtual Window&     	GetWindow() const{DBG_BF_ASSERT(0, "STRIP"); Window aWindow(NULL,0); return aWindow;} //STRIP001 virtual Window&     	GetWindow() const;
+// the variable pFakeWindow is used to avoid
+// warning: reference to local variable ‘aFakeWindow’ returned
+							virtual Window&     	GetWindow() const{DBG_BF_ASSERT(0, "STRIP"); return *pFakeWindow;} //STRIP001 virtual Window&     	GetWindow() const;
 	virtual BOOL			InsertDocument( SfxObjectShell *pDoc){DBG_BF_ASSERT(0, "STRIP"); return FALSE;} //STRIP001 virtual BOOL			InsertDocument( SfxObjectShell *pDoc);
 	virtual void			InsertWindow( Window* ){DBG_BF_ASSERT(0, "STRIP");} //STRIP001 virtual void			InsertWindow( Window* );
 };
