Index: makefile.mk
===================================================================
RCS file: /cvs/gsl/vcl/util/makefile.mk,v
retrieving revision 1.71
diff -u -u -r1.71 makefile.mk
--- vcl/util/makefile.mk	18 Jul 2005 14:10:12 -0000	1.71
+++ vcl/util/makefile.mk	5 Aug 2005 00:59:49 -0000
@@ -298,29 +298,27 @@
             $(SALLIB)
 
 # prepare linking of Xinerama
-.IF "$(USE_XINERAMA)" != "no"
+.IF "$(USE_XINERAMA)" != "NO"
 
-.IF "$(OS)"=="MACOSX"
+.IF "$(OS)" == "MACOSX"
 XINERAMALIBS=-lXinerama
 .ELSE
 .IF "$(OS)" != "SOLARIS"
-.IF "$(CPU)" == "I"
 .IF "$(XINERAMA_LINK)" == "dynamic"
 XINERAMALIBS= -lXinerama
 .ELSE
 XINERAMALIBS= -Wl,-Bstatic -lXinerama -Wl,-Bdynamic 
 .ENDIF # XINERAMA_LINK == dynamic
-.ENDIF # CPU == I
 .ENDIF # OS == SOLARIS
 .ENDIF # OS == MACOSX
 
 SHL2STDLIBS += $(XINERAMALIBS)
 
+.ENDIF # USE_XINERAMA != no
+
 .IF "$(XRENDER_LINK)" == "YES"
 SHL2STDLIBS+=`pkg-config --libs xrender`
 .ENDIF
-
-.ENDIF # USE_XINERAMA != no
 
 .IF "$(ENABLE_PASF)" != ""
 .IF "$(OS)"=="MACOSX"
