--- xmlsecurity/source/xmlsec/makefile.mk	7 Nov 2007 10:06:47 -0000	1.10
+++ xmlsecurity/source/xmlsec/makefile.mk	7 Mar 2008 14:37:05 -0000
@@ -45,6 +45,11 @@ ENABLE_EXCEPTIONS = TRUE
 .INCLUDE :  settings.mk
 .INCLUDE :	$(PRJ)$/util$/target.pmk
 
+.IF "$(WITH_MOZILLA)" == "NO"
+@all:
+	@echo "No mozilla -> no nss -> no libxmlsec -> no xmlsecurity..."
+.ENDIF
+
 .IF "$(SYSTEM_LIBXML)" == "YES"
 CFLAGS+=-DSYSTEM_LIBXML $(LIBXML_CFLAGS)
 .ENDIF
@@ -53,10 +58,6 @@ CFLAGS+=-DSYSTEM_LIBXML $(LIBXML_CFLAGS)
 CDEFS += -DXMLSEC_CRYPTO_MSCRYPTO -DXMLSEC_NO_XSLT
 .ELSE
 CDEFS += -DXMLSEC_CRYPTO_NSS -DXMLSEC_NO_XSLT
-.IF "$(WITH_MOZILLA)" == "NO"
-@all:
-	@echo "No mozilla -> no nss -> no libxmlsec -> no xmlsecurity.."
-.ENDIF
 .ENDIF
 
 # --- Files --------------------------------------------------------
