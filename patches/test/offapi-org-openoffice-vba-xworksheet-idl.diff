--- /dev/null	2004-08-25 23:04:59.000000000 +0530
+++ offapi/org/openoffice/vba/XWorkSheet.idl	2005-05-13 11:56:20.331313503 +0530
@@ -0,0 +1,92 @@
+#ifndef __org_openoffice_vba_XWorkSheet_idl__
+#define __org_openoffice_vba_XWorkSheet_idl__
+
+#ifndef __com_sun_star_uno_XInterface_idl__
+#include <com/sun/star/uno/XInterface.idl>
+#endif
+
+//=============================================================================
+
+module org {  module openoffice {  module vba { 
+
+//=============================================================================
+
+/** 
+ */
+published interface XWorkSheet: com::sun::star::uno::XInterface
+{
+
+	//WorkSheet Properties
+
+	[attribute, readonly] string Name;
+
+	// [attribute, readonly] string Outline; -- Outline object required
+	
+	[attribute] boolean ProtectContents;
+
+	[attribute, readonly] boolean ProtectDrawingObjects;
+
+	[attribute, readonly] boolean ProtectionMode; 
+
+	[attribute, readonly] boolean ProtectScenarios; 
+	
+	[attribute, readonly] long StandardHeight; 
+
+	[attribute, readonly] long StandardWidth;
+
+	//[attribute, readonly] org::openoffice::vba::XRange UsedRange;
+
+	[attribute, readonly] boolean Visible; 
+
+
+	//Methods  Note: Need to filter/prioritize the relevant methods
+
+	/*Activates the specified WorkSheet*/
+	void activate();
+
+	/*Calculates the specified WorkSheet*/
+	void calculate();
+
+	void checkspelling([in] string aCustomDictionary, [in] boolean bIgnoreUpperCase, [in] boolean bAlwaysSuggest, [in] string aSpellLang);
+
+	/*copies the workbook
+
+	Arguments Before or After specified with := operator since both cannot be specified together
+	*/
+	void copy();	
+
+	/*moves the workbook
+	
+	Arguments Before or After specified with := operator since both cannot be specified together	
+	*/
+	void move();	
+
+	/**Delete	*/
+	void deleteSheet();
+
+	/*protect worksheet*/
+	void protect([in] string aPassword, [in] boolean bDrawingObjects, [in] boolean bContents, [in] boolean bScenarios,
+				[in] boolean bUserInterfaceOnly);
+
+	void select();
+
+	void setBackgroundPicture([in] string aFileName);
+
+	void unprotect([in] string aPassword);
+
+
+	//Range 
+
+	// Cell Methods
+
+	//Events like Change Deactivate to be handled seperately?
+
+};
+
+//=============================================================================
+
+}; }; };
+
+#endif
+
+
