--- /dev/null	
+++ offapi/org/openoffice/vba/Excel/Constants.idl	
@@ -0,0 +1,20 @@
+#ifndef __org_openoffice_vba_Excel_Constants_idl__
+#define __org_openoffice_vba_Excel_Constants_idl__
+module org  { module openoffice { module vba { module Excel { 
+
+constants Constants
+{
+const long xlNone = -4142; //(&HFFFFEFD2)
+const long xlAll = -4104; //(&HFFFFEFF8)
+// name space for the following 2 constants is should be just Excel
+// but its not possible for a const group and module to have the same 
+// name ( if there is no id clash here then there is no problem )
+const long xlColorIndexAutomatic = -4105; // (&HFFFFEFF7)
+const long xlColorIndexNone = -4142; // (&HFFFFEFD2)
+
+const long xlAutomatic = -4105; // (&HFFFFEFF7)
+
+};
+}; }; }; };
+
+#endif
