--- sfx2/source/dialog/filedlghelper.cxx.bak	2004-08-24 18:08:05.000000000 +0530
+++ sfx2/source/dialog/filedlghelper.cxx	2004-08-24 18:42:57.000000000 +0530
@@ -472,8 +472,14 @@
 	{
 		aFilterName = xFltMgr->getCurrentFilter();
 
-		if ( aFilterName.Len() && isShowFilterExtensionEnabled() )
-			aFilterName = getFilterName( aFilterName );
+		OUString aFilterDescription(aFilterName);
+		if ( aFilterDescription.lastIndexOf( '*' ) == -1 ) 
+			aFilterName = (String) aFilterDescription.trim();
+		else
+		{
+			if ( aFilterName.Len() && isShowFilterExtensionEnabled() )
+				aFilterName = getFilterName( aFilterName );
+		}
 	}
 
 	return aFilterName;
