--- sfx2/source/dialog/filedlghelper.cxx    	2004-08-24 18:08:05.000000000 +0530
+++ sfx2/source/dialog/filedlghelper.cxx	2004-08-27 10:47:24.562357912 +0530
@@ -472,8 +472,18 @@
 	{
 		aFilterName = xFltMgr->getCurrentFilter();
 
-		if ( aFilterName.Len() && isShowFilterExtensionEnabled() )
-			aFilterName = getFilterName( aFilterName );
+		OUString aFilterDescription( aFilterName );
+
+		// For ex: aFilterDescription == "Text CSV (*.csv;*.txt)", last char = ')' and last but 4th char = '.'
+		if ( ( aFilterDescription.lastIndexOf( ')' ) == ( aFilterDescription.getLength() - 1 ) ) &&
+		     ( aFilterDescription.lastIndexOf( '.' ) == ( aFilterDescription.getLength() - 5 ) ) ) 
+		{
+			if ( aFilterName.Len() && isShowFilterExtensionEnabled() )
+				aFilterName = getFilterName( aFilterName );
+		}
+		// For ex: aFilterDescription == "Text CSV " and doesnt need getFilterName anymore, needs trimming though 
+		else
+			aFilterName = (String) aFilterDescription.trim();
 	}
 
 	return aFilterName;
